# GraphCampus 每日开发规划

> 三人团队 · A（8-10h/天）+ B（3h/天）+ C（3h/天）· 5-6 周
> 本文档为 `GraphCampus_项目规划书.md` 的每日执行落地版

---

## 一、代码总体框架

### 1.1 目录结构（含详细文件功能说明）

```
graphcampus/
│
├── main.py
│   项目入口文件。负责：1) 加载配置 2) 初始化数据库 3) 运行健康检查 4) 启动 Streamlit 服务。
│   是整个应用的启动点，运行 `streamlit run main.py` 即从这里开始。
│
├── requirements.txt
│   Python 依赖清单。列出所有第三方库（streamlit, langgraph, networkx, pypdfium2 等），
│   用于 `pip install -r requirements.txt` 一键安装。
│
├── README.md
│   项目说明文档。Day 1 写入所有接口定义（AgentState / Schema / Model / 表结构），
│   方便新成员快速上手。含环境配置、快速启动指南。
│
├── config/                               # 配置中心 —— 管理所有可调参数
│   ├── config.yaml
│   │   全局 YAML 配置文件。含 LLM 参数（model/temperature/重试次数）、RAG 参数（chunk_size/Top-K）、
│   │   缓存参数（TTL/相似度阈值）、数据库路径、日志级别等。启动时加载，支持运行时热加载。
│   │   文件路径参考：`config/config.yaml`
│   │
│   ├── feature_flags.py
│   │   特征开关。一个全局字典控制每个功能的启用/关闭（如 campus_qa / dual_validation / node2vec 等），
│   │   支持答辩时一键关闭特定功能做对比演示。也支持按用户/环境灰度。
│   │   文件路径参考：`config/feature_flags.py`
│   │
│   └── .env.example
│       环境变量模板。存放敏感配置（如 LLM_API_KEY）的占位文件，开发者复制为 .env 后填入真实值。
│       文件路径参考：`config/.env.example`
│
├── models/                               # Pydantic v2 数据模型 —— 所有数据的"形状"定义
│   ├── __init__.py
│   │   包初始化。导出所有 model 类，方便其他模块 `from models import CourseCreate`。
│   │
│   ├── course.py
│   │   课程相关模型。定义 CourseCreate（创建课程请求体）、CourseResponse（课程响应）、
│   │   Prerequisite（先修关系）等 Pydantic model。含字段校验规则（如课程名 1-100 字符、
│   │   课程代码正则 `^[A-Z]+\d{4}$`、学分 0.5-30 范围）。
│   │   文件路径参考：`models/course.py`
│   │
│   ├── teacher.py
│   │   教师模型。定义 Teacher（姓名/学院/研究方向/个人主页）的 Pydantic model。
│   │   文件路径参考：`models/teacher.py`
│   │
│   ├── lab.py
│   │   实验室模型。定义 Lab（名称/所属学院/研究方向/负责人）的 Pydantic model。
│   │   文件路径参考：`models/lab.py`
│   │
│   ├── event.py
│   │   讲座/竞赛事件模型。定义 Event（标题/类型/时间/地点/报名链接）的 Pydantic model。
│   │   文件路径参考：`models/event.py`
│   │
│   ├── log.py
│   │   日志模型。定义 LogEntry（trace_id/节点名/耗时/详情字典）的结构化日志 model。
│   │   文件路径参考：`models/log.py`
│   │
│   ├── campus.py
│   │   ⭐新增：校园文档/分块模型。定义 CampusDocument（文档ID/类别/标题/内容/来源URL/
│   │   发布日期/有效期/标签/置信度）和 Chunk（分块ID/所属文档/块内容/标题链/位置编号）的
│   │   Pydantic model。这是"校园十万个为什么"功能的数据基础。
│   │   文件路径参考：`models/campus.py`
│   │
│   └── agent_state.py
│       AgentState 定义。LangGraph 的核心数据结构，包含 user_id、messages（滑动窗口20轮）、
│       chat_summary、courses 字典、course_graph（NetworkX 图）、bm25_index、dense_index、
│       cache_pool、campus_docs、campus_indexes、intent 等字段。在所有 Agent 节点间传递。
│       文件路径参考：`models/agent_state.py`
│
├── agents/                               # LangGraph 工作流 —— 项目的"大脑"，编排所有智能体
│   ├── __init__.py
│   │   包初始化。导出所有 Agent 类。
│   │
│   ├── parent_graph.py
│   │   父图编排。整个系统的总入口图，流程：Input → 解析节点 → 意图路由 → Fan-out并行调度
│   │   （规划师/情报官/学伴/百事通）→ Fan-in聚合 → Output。负责子图的创建、编译、调用，
│   │   以及中断点（interrupt_before）的管理。
│   │   文件路径参考：`agents/parent_graph.py`
│   │
│   ├── planner_agent.py
│   │   规划师子图。处理课程图谱相关任务：双路 LLM 提取先修 → Jaccard 交叉验证 →
│   │   置信度分级决策（高中低三级）→ Node2Vec 链路预测 → 幻觉过滤 → 图谱更新 →
│   │   学习路径推荐（拓扑+学期约束+备用）。是整个系统的"课程专家"。
│   │   文件路径参考：`agents/planner_agent.py`
│   │
│   ├── intel_agent.py
│   │   情报官子图。负责 Personalized PageRank 推荐：初始化 personalization vector →
│   │   PPR 迭代传播 → Top-K 排序 → 推理链生成。在课程/教师/实验室/研究方向/讲座五类
│   │   节点的异构图上运行，发现多跳隐性关联。
│   │   文件路径参考：`agents/intel_agent.py`
│   │
│   ├── buddy_agent.py
│   │   学伴子图。负责 Hybrid RAG 问答 + 共情生成：Query Rewriting（多轮上下文压缩）→
│   │   BM25 + Dense 双路检索 → RRF 融合 → LLM 生成带引用回答 → 语义缓存查/写 →
│   │   LLM 共情生成。是学生日常提问的"学习伙伴"。
│   │   文件路径参考：`agents/buddy_agent.py`
│   │
│   └── knowit_agent.py
│       ⭐新增：百事通子图。负责"校园十万个为什么"：意图分类（教务/生活/课程/通用）→
│       分类路由（路由到对应索引）→ 时间感知排序 + 有效期过滤 → 多源融合（跨类别RRF）
│       → 来源引用格式化 → LLM 生成。是校园生活的"万事通"。
│       文件路径参考：`agents/knowit_agent.py`
│
├── curriculum/                           # 课程处理核心算法 —— 规划师 Agent 的算法支撑
│   ├── __init__.py
│   │
│   ├── dual_extractor.py
│   │   双路 LLM 提取器。同时调用两个独立的 LLM（LLM-A + LLM-B）并行分析课程大纲，
│   │   各自提取先修课程列表。返回两份独立提取结果供后续交叉验证。
│   │   核心函数：`DualExtractor.extract(syllabus_text)` → (prereqs_a, prereqs_b)
│   │   文件路径参考：`curriculum/dual_extractor.py`
│   │
│   ├── cross_validator.py
│   │   Jaccard 交叉验证器。计算两路 LLM 提取结果的 Jaccard 相似度，按三级置信度决策：
│   │   =1.0 高置信度（自动入库）、0.5-0.99 中置信度（中断确认）、<0.5 低置信度（手动编辑）。
│   │   核心函数：`CrossValidator.validate(prereqs_a, prereqs_b)` → (confidence, diff_items)
│   │   文件路径参考：`curriculum/cross_validator.py`
│   │
│   ├── node2vec_predictor.py
│   │   Node2Vec 链路预测器。对已有课程图谱训练 Node2Vec 嵌入，计算未直接连接课程对的
│   │   余弦相似度，发现教师可能遗漏的先修关系，主动推荐给用户确认。
│   │   核心函数：`Node2VecPredictor.train(graph)` → `predict_missing()` → [(u,v,score)]
│   │   文件路径参考：`curriculum/node2vec_predictor.py`
│   │
│   └── hallucination_filter.py
│       幻觉过滤器。双重检测：1) 名校验——提取的课程名必须在预置列表中 2) 循环依赖检测——
│       新增先修关系不能形成 A→B→A 环路。任一项不通过则打回。
│       核心函数：`HallucinationFilter.filter(courses, prereqs)` → (passed, rejected_items)
│       文件路径参考：`curriculum/hallucination_filter.py`
│
├── knowledge_graph/                      # 知识图谱 —— 课程关系的"地图"
│   ├── __init__.py
│   │
│   ├── graph_builder.py
│   │   图谱构建器。使用 NetworkX 建立课程/教师/实验室/研究方向的有向图，节点带属性，
│   │   边带权重/置信度。提供 Pyvis HTML 序列化功能，输出可供前端直接嵌入的交互式图谱。
│   │   核心函数：`GraphBuilder.build(courses, relations)` → nx.DiGraph
│   │   `serialize_pyvis(graph)` → HTML string
│   │   文件路径参考：`knowledge_graph/graph_builder.py`
│   │
│   ├── ppr_recommender.py
│   │   Personalized PageRank 推荐器。在异构图上以用户当前关注点为中心分配 personalization
│   │   vector 权重，迭代 PPR 传播后取 Top-K 推荐，并生成推理链（A→B→C 的路径溯源）。
│   │   核心函数：`PPRRecommender.recommend(graph, personalization)` → [(node, score, chain)]
│   │   文件路径参考：`knowledge_graph/ppr_recommender.py`
│   │
│   └── topo_sorter.py
│       拓扑排序器。包含三个功能：1) 拓扑排序得到线性学习顺序 2) 学期约束（每学期最多6门课、
│       先修课必须早于后继课）3) 用户标记冲突后自动生成备用路径。
│       核心函数：`TopoSorter.sort(graph, constraints)` → main_path, alt_paths
│       文件路径参考：`knowledge_graph/topo_sorter.py`
│
├── rag/                                  # 混合检索 RAG —— 学伴 Agent 的知识检索引擎
│   ├── __init__.py
│   │
│   ├── bm25_index.py
│   │   BM25 倒排索引。使用 rank_bm25 库为文档构建关键词倒排索引，支持精确关键词匹配。
│   │   为三类校园数据（教务/生活/课程）分别维护独立的 BM25 实例。
│   │   核心函数：`BM25Index.build(docs)` → `search(query, top_k=5)` → [(doc_id, score)]
│   │   文件路径参考：`rag/bm25_index.py`
│   │
│   ├── dense_index.py
│   │   Dense Embedding 索引。使用 sentence-transformers 将文本转为向量，存入内存或 SQLite，
│   │   检索时计算余弦相似度。支持语义匹配（关键词不同但意思相近也能召回）。
│   │   核心函数：`DenseIndex.build(docs)` → `search(query, top_k=5)` → [(doc_id, score)]
│   │   文件路径参考：`rag/dense_index.py`
│   │
│   ├── hybrid_retriever.py
│   │   混合检索器。Hybrid RAG 的核心：同时走 BM25（关键词）和 Dense（语义）两条路，
│   │   各取 Top-5 → RRF（Reciprocal Rank Fusion, k=60）融合排序 → 取 Top-3 →
│   │   调用 LLM 生成带引用来源的回答。
│   │   核心函数：`HybridRetriever.retrieve(query)` → (answer, sources)
│   │   文件路径参考：`rag/hybrid_retriever.py`
│   │
│   ├── query_rewriter.py
│   │   查询改写器。多轮对话时，LLM 将历史上下文压缩到当前问题中，解决指代消解问题。
│   │   例如："用什么教材？" → "数据结构这门课用什么教材？"
│   │   核心函数：`QueryRewriter.rewrite(query, history)` → rewritten_query
│   │   文件路径参考：`rag/query_rewriter.py`
│   │
│   └── semantic_cache.py
│       语义缓存。以 system_prompt_hash + user_message_embedding 为 Key，余弦相似度 > 0.92
│       命中缓存直接返回，TTL=3600s + LRU 淘汰。同一问题或相似问题秒回，零 LLM 调用。
│       核心函数：`SemanticCache.get(query_emb)` → response or None
│       `SemanticCache.set(query_emb, response)` → 写入缓存
│       文件路径参考：`rag/semantic_cache.py`
│
├── campus_qa/                            # 校园十万个为什么（⭐ 新增核心模块）
│   ├── __init__.py
│   │
│   ├── intent_classifier.py
│   │   意图分类器。用户提问后，先用 LLM 轻量分类确定问题属于哪一类：
│   │   academic（教务类：选课/补考/转专业）、life（生活类：快递/食堂/校医院）、
│   │   course（课程类：大纲/复习/课件）、general（通用类：不确定）。
│   │   核心函数：`IntentClassifier.classify(query)` → intent_label (academic/life/course/general)
│   │   文件路径参考：`campus_qa/intent_classifier.py`
│   │
│   ├── category_router.py
│   │   分类路由器。根据意图分类结果将查询路由到对应的索引引擎：
│   │   academic → 教务通知 BM25+Dense 索引；life → 生活指南索引；
│   │   course → 课程资料索引；general → 全部索引并行检索后加权融合。
│   │   核心函数：`CategoryRouter.route(intent, query)` → [(index_name, results)]
│   │   文件路径参考：`campus_qa/category_router.py`
│   │
│   ├── time_ranker.py
│   │   时间感知排序器。对检索结果按时间衰减加权：score_final = score × exp(-λ × days_ago)，
│   │   λ=0.01。同时过滤已过期的通知（expiry_date < today 则排除）。
│   │   确保最新通知排名最前，过期政策不误导用户。
│   │   核心函数：`TimeRanker.rerank(results)` → [(doc_id, adjusted_score)]
│   │   文件路径参考：`campus_qa/time_ranker.py`
│   │
│   ├── multi_source_fuser.py
│   │   多源融合器。当用户提问跨类别（如"开学要做什么？"同时涉及教务和生活），
│   │   对不同索引返回的结果进行分数归一化（Min-Max）后执行跨类别 RRF 融合排序。
│   │   核心函数：`MultiSourceFuser.fuse(category_results_dict)` → [(doc_id, fused_score)]
│   │   文件路径参考：`campus_qa/multi_source_fuser.py`
│   │
│   └── citation_formatter.py
│       来源引用格式化器。将检索到的来源信息组装为前端可展示的引用卡片格式：
│       [来源: 教务处 → 考务专栏 → 2025-2026-2 补考通知 (第2节)]。
│       同时生成最终回答中的引用标注和来源详情。
│       核心函数：`CitationFormatter.format(sources)` → [citation_card, ...]
│       文件路径参考：`campus_qa/citation_formatter.py`
│
├── data_pipeline/                        # 非结构化数据处理管道（C 负责）
│   ├── __init__.py
│   │
│   ├── doc_parser.py
│   │   文档解析器。支持三种格式：PDF（pypdfium2 提取文本+结构）、Word（python-docx）、
│   │   HTML（BeautifulSoup）。输出纯文本 + 标题层级结构，供分块器使用。
│   │   核心函数：`DocParser.parse(file_path)` → Document(title, headers, text)
│   │   文件路径参考：`data_pipeline/doc_parser.py`
│   │
│   ├── text_cleaner.py
│   │   文本清洗器。对原始文档文本进行：去噪（移除无关字符/页眉页脚）、标准化（统一换行/
│   │   空格/标点）、简单去重（移除完全相同的段落）。提高分块和检索质量。
│   │   核心函数：`TextCleaner.clean(raw_text)` → cleaned_text
│   │   文件路径参考：`data_pipeline/text_cleaner.py`
│   │
│   ├── chunker.py
│   │   智能分块器。Layout-aware 分块策略：检测 Markdown/HTML 标题层级（H1/H2/H3+），
│   │   按标题切分，chunk_size=512 tokens，overlap=50，保持段落/表格/列表完整性不截断。
│   │   每个 chunk 继承父级标题链元数据。
│   │   核心函数：`LayoutAwareChunker.chunk(document)` → [Chunk, ...]
│   │   文件路径参考：`data_pipeline/chunker.py`
│   │
│   ├── metadata_annotator.py
│   │   元数据标注器。用 LLM 辅助标注每个 chunk 的类别（academic/life/course）、
│   │   关键词标签、有效期（如通知的截止日期）。提升检索时的过滤精确度。
│   │   核心函数：`MetadataAnnotator.annotate(chunk)` → chunk_with_metadata
│   │   文件路径参考：`data_pipeline/metadata_annotator.py`
│   │
│   └── index_builder.py
│       索引构建器。将标注后的 chunk 同时构建 BM25 倒排索引和 Dense Embedding 索引，
│       索引按类别（academic/life/course）分开存储。支持增量更新。
│       核心函数：`IndexBuilder.build(chunks)` → (bm25_index, dense_index)
│       文件路径参考：`data_pipeline/index_builder.py`
│
├── review/                               # 课后复习 —— 学生笔记与课程知识关联
│   ├── __init__.py
│   │
│   ├── note_extractor.py
│   │   笔记提取器。学生粘贴/上传课堂笔记后，用 LLM 提取其中的知识点列表，
│   │   同时生成 Embedding 向量供后续图谱匹配。
│   │   核心函数：`NoteExtractor.extract(note_text)` → [KnowledgePoint(name, embedding)]
│   │   文件路径参考：`review/note_extractor.py`
│   │
│   └── knowledge_mapper.py
│       知识映射器。将笔记中提取的知识点 Embedding 与课程图谱中的课程节点做余弦相似度匹配，
│       找到对应的前后继课程，在图谱上标注高亮位置。生成复习/预习建议。
│       核心函数：`KnowledgeMapper.map(knowledge_points, graph)` → highlighted_graph, suggestions
│       文件路径参考：`review/knowledge_mapper.py`
│
├── crawler/                              # 爬虫（B 负责）—— 采集校园公开数据
│   ├── __init__.py
│   │
│   ├── academic_crawler.py
│   │   教务处通知爬虫。从教务处官网/教务系统抓取选课通知、补考安排、转专业政策等，
│   │   支持增量爬取（只爬新发布的页面）。输出 PDF 或 HTML 到 data/raw/academic_notices/。
│   │   核心函数：`AcademicCrawler.crawl(urls)` → 下载文件到 raw 目录
│   │   文件路径参考：`crawler/academic_crawler.py`
│   │
│   ├── life_crawler.py
│   │   生活指南爬虫。从后勤官网、公众号、校内论坛抓取快递点、食堂、校医院、校园卡等
│   │   校园生活信息。支持分页爬取、增量更新。
│   │   核心函数：`LifeCrawler.crawl(urls)` → 下载/整理到 data/raw/life_guides/
│   │   文件路径参考：`crawler/life_crawler.py`
│   │
│   └── utils.py
│       爬虫工具函数。含通用的请求头管理、请求重试、速率控制、HTML 解析辅助函数，
│       供 academic_crawler.py 和 life_crawler.py 共用。
│       文件路径参考：`crawler/utils.py`
│
├── database/                             # 数据库（B 负责建表 + CRUD）
│   ├── __init__.py
│   │
│   ├── connection.py
│   │   数据库连接管理。SQLite 连接池，WAL 模式（高并发读取），启动时自动创建数据库文件，
│   │   运行建表 DDL。提供 get_connection() 上下文管理器。
│   │   核心函数：`get_connection()` → sqlite3.Connection；`init_db()` → 建表
│   │   文件路径参考：`database/connection.py`
│   │
│   ├── schema.py
│   │   数据库表结构 DDL。定义了所有表的 CREATE TABLE 语句：
│   │   courses / teachers / labs / events / logs / cache（语义缓存表）/
│   │   embeddings（向量存储）/ documents（校园文档）/ chunks（文档分块）。
│   │   每张表的字段、类型、索引、外键约束都在这里定义。
│   │   文件路径参考：`database/schema.py`
│   │
│   └── crud.py
│       CRUD 操作封装。为每张表提供统一的增删改查方法，如 CourseCRUD.create() / get() /
│       update() / delete()、DocumentCRUD.search_by_category() 等。
│       文件路径参考：`database/crud.py`
│
├── utils/                                # 工具层（工业化基建——A 的核心职责）
│   ├── __init__.py
│   │
│   ├── llm_client.py
│   │   LLM 中枢。所有 LLM 调用的统一入口。功能链：速率限制检查 → 特征开关检查 → 语义缓存查询
│   │   → 并发槽检查 → 熔断器状态 → 重试(3次,指数退避) → 计时 → 缓存写入。
│   │   支持 OpenAI / 国产大模型切换，提供统一的 call() 接口。
│   │   核心函数：`LLMClient.call(system_prompt, user_message)` → response
│   │   文件路径参考：`utils/llm_client.py`
│   │
│   ├── embedding_client.py
│   │   Embedding 服务封装。封装 sentence-transformers，提供统一的 embed() 接口。
│   │   支持单文本和批量文本向量化。本地运行零 API 成本。
│   │   核心函数：`EmbeddingClient.embed(text)` → List[float]
│   │   `EmbeddingClient.embed_batch(texts)` → List[List[float]]
│   │   文件路径参考：`utils/embedding_client.py`
│   │
│   ├── config_loader.py
│   │   配置加载器。启动时加载 config.yaml，然后检查 .env 环境变量覆盖，支持运行时
│   │   热加载（定期检查文件变更）。提供统一的 get(key) 接口获取任意配置项。
│   │   核心函数：`ConfigLoader.load()` → config dict；`ConfigLoader.get(key)` → value
│   │   文件路径参考：`utils/config_loader.py`
│   │
│   ├── health.py
│   │   健康检查器。检查所有组件的运行状态：数据库连接、LLM 服务连通性、Embedding 服务、
│   │   BM25 索引状态、Dense 索引状态、缓存服务。返回 JSON 格式的健康报告。
│   │   核心函数：`HealthChecker.check()` → {status, checks:{db, llm, embedding, indexes, cache}}
│   │   文件路径参考：`utils/health.py`
│   │
│   ├── rate_limiter.py
│   │   速率限制器。滑动窗口算法实现，默认 10 次请求/分钟/用户，超过返回 429 友好提示。
│   │   支持白名单（答辩演示时免除限流）。
│   │   核心函数：`RateLimiter.check(user_id)` → bool（通过/限流）
│   │   文件路径参考：`utils/rate_limiter.py`
│   │
│   ├── tracer.py
│   │   性能追踪器。每条请求生成唯一 trace_id，记录 LangGraph 每个节点执行前后的时间戳、
│   │   LLM 调用 token 数、检索耗时等，输出 JSON 格式的结构化日志。支持按 trace_id
│   │   追踪单次请求全链路。
│   │   核心函数：`Tracer.start_trace()` → trace_id；`Tracer.log_node(node_name, latency, detail)`
│   │   文件路径参考：`utils/tracer.py`
│   │
│   └── error_codes.py
│       错误码体系。枚举定义 E001-E008 共 8 个标准错误码，每个对应一个错误场景和
│       用户友好的提示文案。确保全系统错误提示统一。
│       文件路径参考：`utils/error_codes.py`
│
├── frontend/                             # Streamlit 前端（B 负责）—— 用户看见和交互的一切
│   ├── __init__.py
│   │
│   ├── app.py
│   │   主应用入口 + 路由。Streamlit 的多页面配置，侧边栏导航，全局状态初始化，
│   │   统一的主题和布局。所有页面的"骨架"。
│   │   文件路径参考：`frontend/app.py`
│   │
│   ├── pages/                            # 多页面 —— 每个文件是一个独立功能页
│   │   ├── __init__.py
│   │   │
│   │   ├── 01_course_map.py
│   │   │   课程地图页。展示完整的课程知识图谱（Pyvis 交互式图），支持搜索课程、
│   │   │   节点高亮、缩放拖拽。点击节点跳转课程详情。含冷启动入口。
│   │   │   文件路径参考：`frontend/pages/01_course_map.py`
│   │   │
│   │   ├── 02_course_detail.py
│   │   │   课程详情页。展示单个课程的完整信息卡（名称/代码/学分/学期/教师），
│   │   │   先修链图谱、后继课程推荐、相关教授。集成双路验证结果对比、链路预测推荐、
│   │   │   路径规划方案 A/B 切换。
│   │   │   文件路径参考：`frontend/pages/02_course_detail.py`
│   │   │
│   │   ├── 03_campus_qa.py
│   │   │   ⭐校园十万个为什么页。核心交互页面：分类 Tab（教务通知/生活指南/课程资料）、
│   │   │   问答输入框、回答展示区（含分类标签、来源引用卡片、时间标签）、热门问题推荐、
│   │   │   空状态/加载态/错误态/边界态处理。
│   │   │   文件路径参考：`frontend/pages/03_campus_qa.py`
│   │   │
│   │   ├── 04_rag_chat.py
│   │   │   RAG 问答对话页。对话消息气泡界面、BM25/Dense 双栏检索结果展示、
│   │   │   引用来源高亮标注、语义缓存命中提示、多轮上下文展示/折叠。
│   │   │   文件路径参考：`frontend/pages/04_rag_chat.py`
│   │   │
│   │   ├── 05_push_panel.py
│   │   │   情报官推送面板。展示 PPR 推荐结果（分数排行 + 推理链溯源），
│   │   │   讲座/竞赛/实验室机会的个性化推荐列表。
│   │   │   文件路径参考：`frontend/pages/05_push_panel.py`
│   │   │
│   │   ├── 06_review.py
│   │   │   课后复习页。笔记粘贴/上传区域、提交按钮、图谱高亮展示区、
│   │   │   复习/预习建议卡片列表。
│   │   │   文件路径参考：`frontend/pages/06_review.py`
│   │   │
│   │   ├── 07_privacy.py
│   │   │   隐私控制页。三种隐私模式切换（离线/标准/严格）、数据流向信息卡、
│   │   │   各数据类型的存储/上传状态一览表。
│   │   │   文件路径参考：`frontend/pages/07_privacy.py`
│   │   │
│   │   └── 08_admin.py
│   │       系统管理页。配置查看/编辑界面、特征开关切换面板、健康状态显示、
│   │       系统日志查看器。
│   │       文件路径参考：`frontend/pages/08_admin.py`
│   │
│   ├── components/                       # 可复用 UI 组件
│   │   ├── __init__.py
│   │   │
│   │   ├── graph_viz.py
│   │   │   图谱可视化组件。封装 Pyvis HTML 的生成和嵌入，提供搜索框、节点高亮、
│   │   │   缩放控制、详情浮窗、知识点高亮等交互功能。在课程地图和课后复习页复用。
│   │   │   文件路径参考：`frontend/components/graph_viz.py`
│   │   │
│   │   ├── chat_ui.py
│   │   │   对话 UI 组件。消息气泡（用户/系统区分）、输入框、消息历史列表、
│   │   │   上下文摘要提示。在 RAG 问答页和十万个为什么页复用。
│   │   │   文件路径参考：`frontend/components/chat_ui.py`
│   │   │
│   │   ├── confidence_tag.py
│   │   │   置信度标签组件。红/黄/绿三色标签展示双路验证的置信度等级：
│   │   │   绿色=高置信度(自动入库)、黄色=中置信度(需确认)、红色=低置信度(需手动编辑)。
│   │   │   文件路径参考：`frontend/components/confidence_tag.py`
│   │   │
│   │   ├── source_card.py
│   │   │   来源引用卡片组件。卡片形式展示检索来源：标题、发布时间、来源分类、
│   │   │   来源 URL、原文片段摘要。在 RAG 问答和十万个为什么页复用。
│   │   │   文件路径参考：`frontend/components/source_card.py`
│   │   │
│   │   └── interrupt_modal.py
│   │       中断交互弹窗组件。确认弹窗（中置信度争议项展示+确认/拒绝按钮）、
│   │       编辑表单（低置信度手动编辑）、链路预测推荐弹窗（相关性分数+确认/拒绝）。
│   │       文件路径参考：`frontend/components/interrupt_modal.py`
│   │
│   └── state_sync.py
│       前端状态同步工具。封装 Streamlit session_state 的读写，提供统一的 set/get 接口，
│       确保不同页面间的状态（如已修课程、当前对话上下文）保持一致。
│       文件路径参考：`frontend/state_sync.py`
│
├── onboarding/                           # 冷启动引导 —— 新用户第一次使用的"向导"
│   ├── __init__.py
│   │
│   └── wizard.py
│       新用户引导流程。5 步向导：1) 选择专业 2) 浏览课程体系 3) 标记已修课程
│       4) 图谱渲染 + Node2Vec 推荐 5) 进入主界面。每步有操作提示和系统行为说明。
│       引导途中随时可退出，下次进入继续。
│       核心函数：`OnboardingWizard.run()` → 分步渲染 UI
│       文件路径参考：`onboarding/wizard.py`
│
├── tests/                                # 测试（C 负责）—— 代码质量的"安检门"
│   ├── __init__.py
│   │
│   ├── conftest.py
│   │   pytest 全局夹具配置。提供：MockLLM（返回预设响应，不真调 API）、
│   │   测试用 SQLite（内存模式，每个测试隔离）、测试 Embedding 夹具、
│   │   预置的 Mock 课程/文档数据。所有测试模块共用。
│   │   文件路径参考：`tests/conftest.py`
│   │
│   ├── test_models.py
│   │   Pydantic model 单元测试。测试所有数据模型的字段校验规则：正常创建、边界值
│   │   （空字符串/超长字符/负值）、正则匹配（课程代码格式）、可选字段默认值。
│   │   文件路径参考：`tests/test_models.py`
│   │
│   ├── test_curriculum/                  # 课程模块测试
│   │   ├── __init__.py
│   │   ├── test_dual_extractor.py
│   │   │   双路提取器测试。Mock LLM 返回预设先修列表，验证提取函数正确解析和返回。
│   │   │   测试单门课和多门课输入、空大纲边界。
│   │   │   文件路径参考：`tests/test_curriculum/test_dual_extractor.py`
│   │   │
│   │   ├── test_cross_validator.py
│   │   │   交叉验证器测试。测试三种置信度场景：完全一致(J=1.0)、部分重叠(J=0.83)、
│   │   │   完全不同(J=0)，验证分级决策逻辑正确性。
│   │   │   文件路径参考：`tests/test_curriculum/test_cross_validator.py`
│   │   │
│   │   ├── test_node2vec.py
│   │   │   Node2Vec 测试。测试训练和预测流程，验证返回的缺失先修格式正确、
│   │   │   分数在合理范围(0-1)。
│   │   │   文件路径参考：`tests/test_curriculum/test_node2vec.py`
│   │   │
│   │   └── test_hallucination_filter.py
│   │       幻觉过滤测试。测试两种检测：课程名不存在列表中应拒绝、循环依赖(A→B→A)
│   │       应检测并拒绝。正常先修关系应通过。
│   │       文件路径参考：`tests/test_curriculum/test_hallucination_filter.py`
│   │
│   ├── test_rag/                         # RAG 模块测试
│   │   ├── __init__.py
│   │   ├── test_bm25.py
│   │   │   BM25 索引测试。测试构建倒排索引、关键词检索返回正确排序结果、
│   │   │   空索引检索返回空列表、增量更新后索引正确。
│   │   │   文件路径参考：`tests/test_rag/test_bm25.py`
│   │   │
│   │   ├── test_dense.py
│   │   │   Dense 索引测试。测试 Embedding 构建和存储、余弦相似度检索返回最相似结果、
│   │   │   语义相似但关键词不同的内容能正确召回。
│   │   │   文件路径参考：`tests/test_rag/test_dense.py`
│   │   │
│   │   ├── test_hybrid.py
│   │   │   混合检索测试。Mock BM25 和 Dense 检索结果，验证 RRF 融合排序逻辑、
│   │   │   Top-3 选择、无结果时空返回处理。
│   │   │   文件路径参考：`tests/test_rag/test_hybrid.py`
│   │   │
│   │   ├── test_query_rewriter.py
│   │   │   查询改写测试。Mock LLM，测试多轮对话中的指代消解、
│   │   │   无历史时的单轮对话直接返回原问题。
│   │   │   文件路径参考：`tests/test_rag/test_query_rewriter.py`
│   │   │
│   │   └── test_semantic_cache.py
│   │       语义缓存测试。测试缓存命中（相似度>0.92）、缓存未命中（相似度<0.92）、
│   │       TTL 过期后自动清除、LRU 淘汰策略、缓存写入和读取一致性。
│   │       文件路径参考：`tests/test_rag/test_semantic_cache.py`
│   │
│   ├── test_campus_qa/                   # 校园十万个为什么测试
│   │   ├── __init__.py
│   │   ├── test_intent.py
│   │   │   意图分类测试。Mock LLM，测试各类输入的分类正确性：教务关键词、生活关键词、
│   │   │   课程关键词、模糊表述（应归为 general）。
│   │   │   文件路径参考：`tests/test_campus_qa/test_intent.py`
│   │   │
│   │   ├── test_router.py
│   │   │   分类路由测试。验证各意图正确路由到对应索引、general 类型路由到全部索引、
│   │   │   空索引时的降级处理。
│   │   │   文件路径参考：`tests/test_campus_qa/test_router.py`
│   │   │
│   │   ├── test_timerank.py
│   │   │   时间排序测试。测试时间衰减加权计算、过期文档被过滤、无日期文档的默认处理、
│   │   │   λ 参数对排序的影响。
│   │   │   文件路径参考：`tests/test_campus_qa/test_timerank.py`
│   │   │
│   │   ├── test_fuser.py
│   │   │   多源融合测试。测试跨类别 RRF 计算、分数归一化（Min-Max）、
│   │   │   单类别输入无需融合、各类别结果分数差距大的情况。
│   │   │   文件路径参考：`tests/test_campus_qa/test_fuser.py`
│   │   │
│   │   └── test_citation.py
│   │       引用格式化测试。测试引用卡片格式生成、长来源截断处理、
│   │       缺少 URL 或日期时的降级格式。
│   │       文件路径参考：`tests/test_campus_qa/test_citation.py`
│   │
│   ├── test_agents/                      # Agent 测试
│   │   ├── __init__.py
│   │   ├── test_planner.py
│   │   │   规划师子图集成测试。Mock 所有依赖，验证完整流程：输入大纲 → 双路提取 →
│   │   │   交叉验证 → 图谱更新 → 路径推荐。验证每一步的 state 变化。
│   │   │   文件路径参考：`tests/test_agents/test_planner.py`
│   │   │
│   │   ├── test_intel.py
│   │   │   情报官子图测试。Mock 图谱，验证 PPR 推荐流程：personalization 构建 →
│   │   │   PPR 迭代 → Top-K 排序 → 推理链生成。
│   │   │   文件路径参考：`tests/test_agents/test_intel.py`
│   │   │
│   │   ├── test_buddy.py
│   │   │   学伴子图测试。Mock 检索和 LLM，验证完整流程：用户提问 → Query Rewriting →
│   │   │   BM25+Dense 检索 → RRF → LLM 生成 → 语义缓存检查。
│   │   │   文件路径参考：`tests/test_agents/test_buddy.py`
│   │   │
│   │   └── test_knowit.py
│   │       百事通子图测试。Mock 分类和检索，验证完整流程：用户提问 → 意图分类 →
│   │       分类路由 → 时间排序 → 多源融合 → 引用格式化 → LLM 生成。
│   │       文件路径参考：`tests/test_agents/test_knowit.py`
│   │
│   ├── test_database/                    # 数据库测试
│   │   ├── __init__.py
│   │   ├── test_schema.py
│   │   │   表结构测试。验证所有表的 CREATE TABLE 语句可执行、字段类型正确、
│   │   │   索引和外键约束创建成功。
│   │   │   文件路径参考：`tests/test_database/test_schema.py`
│   │   │
│   │   └── test_crud.py
│   │       CRUD 测试。测试每张表的增删改查操作：创建记录、读取记录、更新字段、
│   │       删除记录、条件查询（按类别/日期/关键词过滤）。
│   │       文件路径参考：`tests/test_database/test_crud.py`
│   │
│   ├── test_data_pipeline/               # 数据管道测试
│   │   ├── __init__.py
│   │   ├── test_parser.py
│   │   │   文档解析测试。分别测试 PDF/Word/HTML 三种格式的解析正确性、
│   │   │   损坏文件/空文件的错误处理、编码问题处理。
│   │   │   文件路径参考：`tests/test_data_pipeline/test_parser.py`
│   │   │
│   │   ├── test_chunker.py
│   │   │   分块器测试。测试按标题切分正确性、chunk_size 和 overlap 参数有效性、
│   │   │   段落/表格/列表完整性保障、无标题文档的 fallback 分块策略。
│   │   │   文件路径参考：`tests/test_data_pipeline/test_chunker.py`
│   │   │
│   │   └── test_index_builder.py
│   │       索引构建测试。验证 BM25 和 Dense 索引构建成功、增量更新正确、
│   │       空文档列表的边界处理。
│   │       文件路径参考：`tests/test_data_pipeline/test_index_builder.py`
│   │
│   └── test_utils/                       # 工具测试
│       ├── __init__.py
│       ├── test_llm_client.py
│       │   LLM 客户端测试。测试重试逻辑（首次失败后重试）、熔断器（连续失败后快速拒绝）、
│       │   并发控制（超过 max=3 时排队）、速率限制（超限时拒绝）。
│       │   文件路径参考：`tests/test_utils/test_llm_client.py`
│       │
│       ├── test_config.py
│       │   配置加载测试。测试 YAML 加载、环境变量覆盖、热加载检测、
│       │   配置缺失时使用默认值。
│       │   文件路径参考：`tests/test_utils/test_config.py`
│       │
│       ├── test_rate_limiter.py
│       │   速率限制测试。测试滑动窗口计数正确性、超限后拒绝、窗口滑动后恢复、
│       │   白名单用户豁免。
│       │   文件路径参考：`tests/test_utils/test_rate_limiter.py`
│       │
│       └── test_tracer.py
│           追踪器测试。测试 trace_id 生成唯一性、各节点打点记录正确性、
│           JSON 日志格式验证。
│           文件路径参考：`tests/test_utils/test_tracer.py`
│
├── scripts/                              # 脚本工具（C 负责）
│   ├── check_data_quality.py
│   │   数据质量检查脚本。检查：分块完整性（无遗漏文本）、Embedding 质量（无零向量）、
│   │   索引覆盖率（所有文档都被索引）、元数据完整率。输出质量报告 JSON。
│   │   运行方式：`python -m scripts.check_data_quality`
│   │   文件路径参考：`scripts/check_data_quality.py`
│   │
│   ├── build_indexes.py
│   │   批量索引构建脚本。遍历 data/processed/chunks/ 目录，重新构建所有 BM25 和
│   │   Dense 索引。数据更新后运行此脚本重建索引。
│   │   运行方式：`python -m scripts.build_indexes`
│   │   文件路径参考：`scripts/build_indexes.py`
│   │
│   └── import_data.py
│       数据导入脚本。将 data/raw/ 中的原始数据经过 data_pipeline 处理后导入数据库。
│       支持全量导入和增量导入。初始化环境时运行。
│       运行方式：`python -m scripts.import_data`
│       文件路径参考：`scripts/import_data.py`
│
├── data/                                 # 数据目录 —— 所有数据文件的"仓库"
│   ├── raw/                              # 原始数据（直接下载/收集的文件）
│   │   ├── academic_notices/
│   │   │   教务处通知 PDF/HTML。从教务处官网爬取的历年通知原文，文件名格式：
│   │   │   `{日期}_{标题}.pdf`，如 `2026-02-20_补考通知.pdf`。
│   │   │   文件路径参考：`data/raw/academic_notices/`
│   │   │
│   │   ├── life_guides/
│   │   │   生活指南文件。快递点、食堂、校医院等校园生活信息，格式为 Markdown 或 JSON。
│   │   │   文件名格式：`{类别}_{标题}.md`，如 `快递_北区快递站指南.md`。
│   │   │   文件路径参考：`data/raw/life_guides/`
│   │   │
│   │   └── course_materials/
│   │       课程资料文件。按课程分文件夹存放：课程大纲 PDF、课件 PPT、往年试题 PDF、
│   │       复习资料等。文件夹名格式：`{课程代码}_{课程名}`。
│   │       文件路径参考：`data/raw/course_materials/`
│   │
│   ├── processed/                        # 处理后数据（管道输出）
│   │   ├── chunks/
│   │   │   分块数据 JSON。每个原始文档经过分块后，每个 chunk 存为一个 JSON 文件，
│   │   │   包含文本内容、标题链、元数据等。供索引构建使用。
│   │   │   文件路径参考：`data/processed/chunks/`
│   │   │
│   │   └── indexes/
│   │       索引缓存。BM25 和 Dense 索引的序列化缓存文件，加速下次启动加载。
│   │       文件路径参考：`data/processed/indexes/`
│   │
│   └── graphcampus.db
│       SQLite 数据库文件。所有结构化数据（课程/教师/关系）和向量数据（Embedding）的
│       持久化存储。WAL 模式支持高并发读取。
│       文件路径参考：`data/graphcampus.db`
│
├── docker/                               # Docker 容器化
│   ├── Dockerfile
│   │   多阶段构建文件。第一阶段安装依赖和模型，第二阶段精简运行环境。
│   │   目标：镜像 < 500MB，启动时间 < 30s。
│   │   文件路径参考：`docker/Dockerfile`
│   │
│   └── .dockerignore
│       Docker 忽略文件。排除 venv/、data/raw/、__pycache__/、.git/ 等不必要的文件，
│       减小构建上下文体积。
│       文件路径参考：`docker/.dockerignore`
│
└── .github/                              # GitHub 自动化
    └── workflows/
        └── ci.yml
            GitHub Actions CI 流程。PR 触发：ruff lint → pytest --cov>80% →
            docker build → 数据质量门禁。main 分支触发：额外执行 deploy（占位）。
            文件路径参考：`.github/workflows/ci.yml`
```

### 1.2 模块依赖关系

```
main.py
  ├── config/          (配置中心，被所有模块依赖)
  ├── models/          (数据模型，被所有模块依赖)
  ├── utils/           (工具层，被所有模块依赖)
  ├── database/        (数据库，被 agents/data_pipeline 依赖)
  │
  ├── frontend/        (前端页面)
  │   ├── pages/       (各页面)
  │   └── components/  (可复用组件)
  │
  ├── agents/          (LangGraph 工作流)
  │   ├── parent_graph.py
  │   ├── planner_agent.py
  │   │   ├── curriculum/         (双路验证 + Node2Vec)
  │   │   ├── knowledge_graph/    (NetworkX + PPR + 拓扑)
  │   │   └── review/             (课后复习)
  │   ├── intel_agent.py
  │   │   └── knowledge_graph/ppr_recommender.py
  │   ├── buddy_agent.py
  │   │   └── rag/                (Hybrid RAG + 缓存)
  │   └── knowit_agent.py         (⭐新增)
  │       └── campus_qa/          (意图分类 + 路由 + 排序 + 融合)
  │
  ├── data_pipeline/   (非结构化数据处理)
  │   ├── doc_parser.py → text_cleaner.py → chunker.py
  │   └── metadata_annotator.py → index_builder.py
  │
  ├── crawler/         (爬虫)
  ├── onboarding/      (冷启动)
  ├── tests/           (测试)
  └── scripts/         (脚本)
```

### 1.3 核心数据流

```
[用户输入]
    │
    ▼
frontend/pages/              ← Streamlit 渲染
    │
    ▼
agents/parent_graph.py       ← LangGraph 入口
    │
    ├── 路由判断（课程问题 → 规划师/情报官/学伴；校园问题 → 百事通）
    │
    ├── agents/planner_agent.py
    │   ├── curriculum/dual_extractor.py       ← LLM-A + LLM-B
    │   ├── curriculum/cross_validator.py       ← Jaccard
    │   ├── curriculum/node2vec_predictor.py   ← Node2Vec
    │   ├── knowledge_graph/graph_builder.py   ← NetworkX
    │   └── knowledge_graph/topo_sorter.py     ← 拓扑
    │
    ├── agents/intel_agent.py
    │   └── knowledge_graph/ppr_recommender.py ← PPR
    │
    ├── agents/buddy_agent.py
    │   ├── rag/query_rewriter.py              ← LLM 改写
    │   ├── rag/hybrid_retriever.py            ← BM25 + Dense → RRF
    │   └── rag/semantic_cache.py              ← 缓存
    │
    └── agents/knowit_agent.py                 ← ⭐ 新增
        ├── campus_qa/intent_classifier.py     ← 分类
        ├── campus_qa/category_router.py       ← 路由
        ├── campus_qa/time_ranker.py           ← 时间排序
        ├── campus_qa/multi_source_fuser.py    ← 融合
        └── campus_qa/citation_formatter.py    ← 引用
    │
    ▼
[响应 → frontend 展示]
```

---

## 二、每日详细安排

### 📅 第 1 周：基建 + 接口锁死 + 数据管道

---

#### Day 1 — 三人共同锁死接口

| 时间 | 角色 | 任务 | 产出文件 |
|------|------|------|---------|
| 全天 | A+B+C | 三人共同讨论并确定所有接口定义 | 见下方 |

**A 具体产出：**
- 定义 AgentState → `graphcampus/models/agent_state.py`
- 定义 JSON Schema（四 Agent 入参出参）→ `graphcampus/models/` 各 model 文件
- 定义配置中心骨架 → `graphcampus/config/config.yaml` + `graphcampus/config/feature_flags.py` + `graphcampus/config/.env.example`
- 项目初始化 → `graphcampus/main.py`（骨架）、`graphcampus/requirements.txt`

**B 具体产出：**
- 数据库表结构设计 → `graphcampus/database/schema.py`（DDL：courses / teachers / labs / events / logs / cache / embeddings / documents / chunks）
- Mock 数据 15 门课 → `graphcampus/data/mock_courses.json`

**C 具体产出：**
- Pydantic model 实现 → `graphcampus/models/course.py`、`graphcampus/models/campus.py`（含 CampusDocument / Chunk）
- Mock 校园文档 30 篇 → `graphcampus/data/mock_campus_docs.json`

**三人共同产出：**
- `graphcampus/README.md` — 写入 Day 1 所有接口定义

**里程碑检查：** Day 1 结束时，所有接口定义锁死，后续不得修改接口签名。

---

#### Day 2

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | LLM 调用封装 | `graphcampus/utils/llm_client.py` | `LLMClient.call()` 含重试（3次指数退避）、熔断器（5错误→30s）、并发控制（max=3）、速率限制（10/min） |
| | | | `graphcampus/utils/embedding_client.py` | `EmbeddingClient.embed(text)` 封装 sentence-transformers |
| | | | `graphcampus/utils/rate_limiter.py` | `RateLimiter.check(user_id)` 滑动窗口算法 |
| **B** | 3h | Streamlit 骨架 | `graphcampus/frontend/app.py` | Streamlit 主入口 + 侧边栏导航路由 |
| | | | `graphcampus/frontend/pages/__init__.py` | 页面包初始化 |
| | | | `graphcampus/frontend/pages/01_course_map.py` | 课程地图页骨架（占位） |
| | | | `graphcampus/frontend/pages/03_campus_qa.py` | 校园十万个为什么页骨架（占位） |
| | | | `graphcampus/frontend/state_sync.py` | session_state 同步工具 |
| **C** | 3h | 数据管道原型 | `graphcampus/data_pipeline/doc_parser.py` | `DocParser.parse(path)` 支持 PDF(pypdfium2) / Word(python-docx) / HTML(bs4) |
| | | | `graphcampus/data_pipeline/text_cleaner.py` | `TextCleaner.clean(text)` 去噪、标准化、简单去重 |

---

#### Day 3

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 日志系统 + 配置中心 | `graphcampus/utils/tracer.py` | `Tracer` 类，生成 trace_id，进出节点自动打点，JSON 格式输出 |
| | | | `graphcampus/utils/config_loader.py` | `ConfigLoader.load()` 加载 YAML → 环境变量覆盖 → 运行时热加载 |
| | | | `graphcampus/utils/error_codes.py` | 错误码枚举 E001-E008 |
| **B** | 3h | 数据库建表 | `graphcampus/database/connection.py` | `get_connection()` SQLite WAL 模式，`init_db()` 启动时初始化 |
| | | | `graphcampus/database/schema.py` | 完善所有 CREATE TABLE 语句 |
| | | | `graphcampus/database/crud.py` | `CourseCRUD`、`DocumentCRUD` 基础增删改查 |
| **C** | 3h | 智能分块 | `graphcampus/data_pipeline/chunker.py` | `LayoutAwareChunker.chunk(text)` 按标题层级切分，chunk_size=512, overlap=50，保持段落/表格/列表完整性 |

---

#### Day 4

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 工业化基建 | `graphcampus/utils/health.py` | `health_check()` 数据库/LLM/Embedding/索引/缓存全组件检查 |
| | | | `graphcampus/main.py` | 完善入口：启动时健康检查 + 配置加载 + 数据库初始化 |
| **B** | 3h | 中断交互组件 | `graphcampus/frontend/components/interrupt_modal.py` | 弹窗组件（确认 / 编辑 / 取消） |
| | | | `graphcampus/frontend/components/__init__.py` | 组件包初始化 |
| **C** | 3h | 元数据标注 | `graphcampus/data_pipeline/metadata_annotator.py` | `MetadataAnnotator.annotate(chunk, doc)` LLM 辅助标注类别/关键词/有效期 |
| | | | `graphcampus/data_pipeline/index_builder.py` | `IndexBuilder.build(docs)` BM25 + Dense 索引构建框架 |
| | | | `graphcampus/scripts/check_data_quality.py` | 数据质量检查脚本（分块完整性、覆盖率） |

---

#### Day 5

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 知识图谱 | `graphcampus/knowledge_graph/graph_builder.py` | `GraphBuilder.build(courses)` NetworkX 有向图构建，`serialize_pyvis()` Pyvis HTML 生成 |
| | | | `graphcampus/knowledge_graph/__init__.py` | 包初始化 |
| **B** | 3h | 课程详情 + 图谱界面 | `graphcampus/frontend/pages/02_course_detail.py` | 课程信息卡（名称/代码/学分/先修/后继/教师） |
| | | | `graphcampus/frontend/components/graph_viz.py` | 图谱可视化组件（Pyvis HTML 嵌入 + 搜索/高亮/缩放） |
| **C** | 3h | 数据收集开始 | `graphcampus/data/raw/academic_notices/` | 从教务处官网下载 50 篇通知 PDF |
| | | | `graphcampus/data/raw/life_guides/` | 整理生活指南（公众号/后勤官网） |

---

#### Day 6

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 拓扑排序 + Node2Vec | `graphcampus/knowledge_graph/topo_sorter.py` | `TopoSorter.sort(graph)` 拓扑排序 + `apply_semester_constraint()` 学期约束 + `generate_alt_paths()` 备用路径 |
| | | | `graphcampus/curriculum/node2vec_predictor.py` | 占位（基础链路，下周完善） |
| **B** | 3h | 冷启动引导 | `graphcampus/onboarding/wizard.py` | `OnboardingWizard` 步骤：选专业 → 课程列表 → 标记已修 → 图谱渲染 |
| | | | `graphcampus/frontend/pages/01_course_map.py` | 完善：集成图谱 + 冷启动入口 |
| **C** | 3h | 数据收集 + 索引构建 | `graphcampus/data/raw/course_materials/` | 整理 10 门课课程大纲 |
| | | | `graphcampus/data_pipeline/index_builder.py` | 完善：首批数据 BM25 索引构建 |

---

#### Day 7 — 周联调

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 周联调主导 | `graphcampus/agents/parent_graph.py` | 父图骨架（串联解析 → 路由 → 子图调度 → 聚合） |
| | | | 联调：录入课程 → LLM 提取 → 图谱构建 → 前端展示 | |
| **B** | 3h | Mock 数据 + 联调配合 | `graphcampus/data/mock_courses.json` | 完善 15 门课（含先修关系、教师信息） |
| | | | 修复前端与后端对接问题 | |
| **C** | 3h | 数据质量 + 联调配合 | `graphcampus/scripts/check_data_quality.py` | 完善：首次数据质量报告 |
| | | | 验证索引构建正确性 | |

**周末 Review：** A 主持会议，检查第一周所有产出是否达标。

---

### 📅 第 2 周：双路验证 + Node2Vec + PPR + 校园数据就绪

---

#### Day 8

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 双路 LLM 提取 | `graphcampus/curriculum/dual_extractor.py` | `DualExtractor.extract(syllabus)` 并行调用 LLM-A + LLM-B，`extract_prerequisites()` 提取先修课程 |
| | | | 集成 `utils/llm_client.py` | |
| **B** | 3h | 双路结果 UI | `graphcampus/frontend/components/confidence_tag.py` | 置信度标签组件（红/黄/绿） |
| | | | 修改 `graphcampus/frontend/pages/02_course_detail.py` | 集成双路提取结果对比视图 |
| **C** | 3h | 教务通知索引 | `graphcampus/data/raw/academic_notices/` | 继续下载 50 篇（累计 100 篇） |
| | | | `graphcampus/data_pipeline/index_builder.py` | 教务通知 BM25 + Dense 索引构建 |

---

#### Day 9

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | Jaccard 验证 + Node2Vec | `graphcampus/curriculum/cross_validator.py` | `CrossValidator.validate(a_prereqs, b_prereqs)` Jaccard 计算 + 三级置信度决策 |
| | | | `graphcampus/curriculum/hallucination_filter.py` | `HallucinationFilter.filter(courses)` 名校验 + 循环依赖检测 |
| | | | `graphcampus/curriculum/node2vec_predictor.py` | 完善：`Node2VecPredictor.train(graph)` 学习嵌入 + `predict_missing()` 发现缺失先修 |
| **B** | 3h | 链路预测 UI | 修改 `graphcampus/frontend/components/interrupt_modal.py` | 链路预测推荐弹窗（显示相关性分数 + 确认/拒绝按钮） |
| | | | 修改 `graphcampus/frontend/pages/02_course_detail.py` | 集成链路预测推荐 |
| **C** | 3h | 生活指南索引 | `graphcampus/data/raw/life_guides/` | 整理生活指南（累计 50+ 篇） |
| | | | `graphcampus/data_pipeline/chunker.py` | 分块质量评估 + 调优 |

---

#### Day 10

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | PPR 实现 | `graphcampus/knowledge_graph/ppr_recommender.py` | `PPRRecommender.recommend(graph, personalization_vector)` 异构图上 PPR 传播，`generate_reasoning_chain()` 路径溯源 |
| **B** | 3h | PPR 推荐前端 | `graphcampus/frontend/pages/05_push_panel.py` | 情报官推送面板：PPR 分数排行 + 推理链展示 |
| **C** | 3h | 课程资料索引 | `graphcampus/data/raw/course_materials/` | 首批 10 门课课程资料整理 |
| | | | `graphcampus/data_pipeline/index_builder.py` | 课程资料索引构建 |

---

#### Day 11

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 路径规划完善 | `graphcampus/knowledge_graph/topo_sorter.py` | 完善：拓扑 → 学期约束 → 备用路径（冲突检测 + 自动切换） |
| **B** | 3h | 路径规划前端 | 修改 `graphcampus/frontend/pages/02_course_detail.py` | 路径规划展示（方案 A/B 切换、路径对比） |
| **C** | 3h | 数据采集补充 | `graphcampus/data/raw/academic_notices/` | 继续下载 50 篇（累计 150 篇，目标 200+） |
| | | | `graphcampus/data/raw/life_guides/` | 补充生活指南（累计 80+ 篇，目标 100+） |

---

#### Day 12 — ⭐ 百事通子图启动

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 百事通：意图分类 + 路由 | `graphcampus/campus_qa/intent_classifier.py` | `IntentClassifier.classify(query)` LLM 轻量分类 → academic / life / course / general |
| | | | `graphcampus/campus_qa/category_router.py` | `CategoryRouter.route(intent, query)` 路由到对应索引，通用类全部检索 |
| **B** | 3h | 校园十万个为什么 UI | `graphcampus/frontend/pages/03_campus_qa.py` | 基础布局：分类 Tab（教务/生活/课程）+ 热门问题推荐卡片 |
| **C** | 3h | 数据管道测试 | `graphcampus/tests/test_data_pipeline/test_parser.py` | 文档解析器单元测试 |
| | | | `graphcampus/tests/test_data_pipeline/test_chunker.py` | 分块器单元测试 |
| | | | `graphcampus/tests/test_data_pipeline/test_index_builder.py` | 索引构建测试 |

---

#### Day 13

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 百事通：时间排序 + 多源融合 | `graphcampus/campus_qa/time_ranker.py` | `TimeRanker.rerank(results)` 时间衰减加权 `score × exp(-λ × days_ago)` + 有效期过滤 |
| | | | `graphcampus/campus_qa/multi_source_fuser.py` | `MultiSourceFuser.fuse(category_results)` 跨类别 RRF + 分数归一化 |
| **B** | 3h | 十万个为什么问答 UI | 修改 `graphcampus/frontend/pages/03_campus_qa.py` | 问答输入框 + 回答展示区 + 加载状态 |
| | | | `graphcampus/frontend/components/source_card.py` | 来源引用卡片组件（标题+日期+URL+原文片段） |
| **C** | 3h | 质量评估 | `graphcampus/tests/test_campus_qa/test_chunker_quality.py` | 分块质量评估测试 |
| | | | `graphcampus/scripts/check_data_quality.py` | 完善：检索 MRR/Recall 评估 |

---

#### Day 14 — 周联调

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 联调主导 | `graphcampus/agents/parent_graph.py` | 完善：集成百事通子图路由 |
| | | | `graphcampus/agents/knowit_agent.py` | 百事通子图组装（intent → route → rank → fuse → cite → LLM） |
| | | | Bug 清理 | |
| **B** | 3h | UI 完善 | 完善 `graphcampus/frontend/pages/03_campus_qa.py` | 空状态、加载态、错误态处理 |
| | | | 联调配合：前后端对接 | |
| **C** | 3h | 完整数据入库 | `graphcampus/data/graphcampus.db` | 导入 40 门课完整数据 + 三类校园数据（通知 200+、生活 100+、课程资料 10 门） |
| | | | 数据质量报告 | |

**周末 Review：** 检查双路验证 + PPR + 百事通基础链路是否跑通。

---

### 📅 第 3 周：Hybrid RAG + 语义缓存 + 百事通完整

---

#### Day 15

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | BM25 索引 | `graphcampus/rag/bm25_index.py` | `BM25Index.build(docs)` rank_bm25 库构建倒排索引，`search(query, top_k=5)` 检索 |
| | | | 支持分类索引：academic / life / course 各一个 BM25 实例 | |
| **B** | 3h | RAG 对话 UI | `graphcampus/frontend/pages/04_rag_chat.py` | 对话界面（消息气泡、输入框、历史记录） |
| | | | `graphcampus/frontend/components/chat_ui.py` | 可复用的对话 UI 组件 |
| | | | 修改 `graphcampus/frontend/pages/03_campus_qa.py` | 接入百事通后端，显示分类结果 |
| **C** | 3h | BM25 测试 | `graphcampus/tests/test_rag/test_bm25.py` | BM25 索引正确性测试（构建/检索/空结果） |
| | | | 完善 `graphcampus/tests/conftest.py` | Mock 文档夹具 |

---

#### Day 16

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | Dense 索引 | `graphcampus/rag/dense_index.py` | `DenseIndex.build(docs)` sentence-transformers 生成 Embedding，`search(query, top_k=5)` 余弦相似度检索 |
| | | | 支持分类索引：academic / life / course 各一个索引 | |
| **B** | 3h | 双栏展示 | 修改 `graphcampus/frontend/pages/04_rag_chat.py` | BM25 / Dense 双栏结果展示 |
| | | | 修改 `graphcampus/frontend/pages/03_campus_qa.py` | 十万个为什么：展示检索来源条数 |
| **C** | 3h | Embedding 质量 | `graphcampus/tests/test_rag/test_dense.py` | Dense 索引正确性测试 |
| | | | `graphcampus/scripts/check_data_quality.py` | 完善：Embedding 相似度分布检查 + 异常检测 |

---

#### Day 17

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | RRF + LLM 生成 | `graphcampus/rag/hybrid_retriever.py` | `HybridRetriever.retrieve(query)` BM25 Top-5 + Dense Top-5 → RRF(k=60) → Top-3 → LLM 生成带引用回答 |
| **B** | 3h | 引用标注 UI | 修改 `graphcampus/frontend/pages/04_rag_chat.py` | 引用标注（来源高亮 + 可点击跳转） |
| | | | 修改 `graphcampus/frontend/pages/03_campus_qa.py` | 十万个为什么：分类标签显示 |
| **C** | 3h | RAG 集成测试 | `graphcampus/tests/test_rag/test_hybrid.py` | Hybrid RAG 全流程集成测试（Mock LLM） |
| | | | `graphcampus/tests/conftest.py` | 完善 Mock LLM 夹具 |

---

#### Day 18

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | Query Rewriting | `graphcampus/rag/query_rewriter.py` | `QueryRewriter.rewrite(query, history)` LLM 上下文压缩，如"用什么教材？"→"数据结构用什么教材？" |
| **B** | 3h | 多轮对话 UI | 修改 `graphcampus/frontend/pages/04_rag_chat.py` | 多轮对话上下文展示（历史消息折叠/展开） |
| | | | 修改 `graphcampus/frontend/pages/03_campus_qa.py` | 十万个为什么多轮上下文支持 |
| **C** | 3h | 十万个为什么集成测试 | `graphcampus/tests/test_campus_qa/test_intent.py` | 意图分类测试 |
| | | | `graphcampus/tests/test_campus_qa/test_router.py` | 分类路由测试 |
| | | | `graphcampus/tests/test_campus_qa/test_timerank.py` | 时间排序测试 |
| | | | `graphcampus/tests/test_campus_qa/test_fuser.py` | 多源融合测试 |

---

#### Day 19

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 语义缓存 | `graphcampus/rag/semantic_cache.py` | `SemanticCache` 类：`get(query_embedding)` 余弦相似度 > 0.92 命中 + `set(query, response)` TTL=3600s + LRU 淘汰 |
| **B** | 3h | 缓存命中提示 | 修改 `graphcampus/frontend/pages/04_rag_chat.py` | 缓存命中提示标签 + 清零按钮 |
| | | | 修改 `graphcampus/frontend/pages/03_campus_qa.py` | 十万个为什么缓存命中提示 |
| **C** | 3h | 缓存测试 | `graphcampus/tests/test_rag/test_semantic_cache.py` | 缓存命中/未命中/TTL/LRU/边界测试 |
| | | | 完善 `graphcampus/tests/conftest.py` | 测试 Embedding 夹具 |

---

#### Day 20

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 全链路联调 | 联调 RAG + 缓存 + Query Rewriting + 百事通全流程 | |
| | | | 修复联调中发现的问题 | |
| **B** | 3h | 引用卡片 UI | 完善 `graphcampus/frontend/pages/03_campus_qa.py` | 十万个为什么来源引用卡片（标题+日期+来源+原文片段） |
| | | | `graphcampus/frontend/components/source_card.py` | 完善卡片样式 |
| **C** | 3h | 端到端测试 | `graphcampus/tests/test_e2e/` | 用户操作路径端到端测试（Mock LLM） |
| | | | `graphcampus/tests/conftest.py` | 完善端到端测试夹具 |

---

#### Day 21

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 学伴子图完整化 | `graphcampus/agents/buddy_agent.py` | 完整学伴子图：RAG + 共情生成 + 行为统计 |
| | | | 共情生成：`generate_empathy(user_context, query)` LLM + 行为上下文 | |
| **B** | 3h | 学伴对话界面 | 完善 `graphcampus/frontend/pages/04_rag_chat.py` | 学伴对话界面（头像、语气、共情提示） |
| | | | 添加行为日志埋点（点击/提问/切换等） | |
| **C** | 3h | 测试报告 | `graphcampus/tests/test_report_week3.md` | 第三周测试报告 + Bug 汇总 |
| | | | `graphcampus/tests/test_agents/test_buddy.py` | 学伴子图集成测试 |

**周末 Review：** Hybrid RAG 全流程验收，语义缓存命中演示。

---

### 📅 第 4 周：课后复习 + 并行 + 隐私 + 工业化

---

#### Day 22

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 课后复习笔记提取 | `graphcampus/review/note_extractor.py` | `NoteExtractor.extract(note_text)` LLM 提取知识点 + Embedding 向量化 |
| **B** | 3h | 课后复习界面 | `graphcampus/frontend/pages/06_review.py` | 笔记粘贴/上传界面 + 提交按钮 |
| **C** | 3h | 课后复习测试 | `graphcampus/tests/test_review/test_note_extractor.py` | 笔记提取单元测试（Mock LLM） |

---

#### Day 23

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 知识点→图谱映射 | `graphcampus/review/knowledge_mapper.py` | `KnowledgeMapper.map(embeddings, graph)` 匹配到图谱中前后继课程 → 标注位置 |
| **B** | 3h | 图谱高亮 + 建议卡 | 修改 `graphcampus/frontend/components/graph_viz.py` | 知识点高亮 |
| | | | 修改 `graphcampus/frontend/pages/06_review.py` | 复习/预习建议卡片 |
| **C** | 3h | 补充课程资料 | `graphcampus/data/raw/course_materials/` | 新增 10 门课课程资料（累计 20 门） |

---

#### Day 24

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 滑动窗口 + 摘要 | 修改 `graphcampus/models/agent_state.py` | 滑动窗口 max_turns=20 |
| | | | 修改 `graphcampus/agents/parent_graph.py` | `generate_summary()` LLM 压缩历史生成摘要 |
| **B** | 3h | 摘要提示 UI | 修改 `graphcampus/frontend/components/chat_ui.py` | 上下文摘要提示（"已为您总结最近对话..."） |
| **C** | 3h | 滑动窗口测试 | `graphcampus/tests/test_agents/test_summary.py` | 滑动窗口 + 摘要测试 |

---

#### Day 25

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | Fan-out / Fan-in 并行 | 修改 `graphcampus/agents/parent_graph.py` | Fan-out 并行调度四子图 + Fan-in 聚合结果 |
| | | | 异步编排：规划师/情报官/学伴/百事通并行执行 | |
| **B** | 3h | 并行协作前端 | 修改 `graphcampus/frontend/app.py` | 并行协作聚合展示（分栏显示各 Agent 结果） |
| **C** | 3h | Fan-out/Fan-in 测试 | `graphcampus/tests/test_agents/test_parallel.py` | Fan-out/Fan-in 全流程集成测试 |

---

#### Day 26

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 隐私模式 | 修改 `graphcampus/models/agent_state.py` | 添加 `privacy_mode` 字段 |
| | | | 修改 `graphcampus/utils/llm_client.py` | 私有模式降级：离线→纯规则+BM25，严格→每次确认 |
| | | | 修改 `graphcampus/config/feature_flags.py` | 添加 `offline_mode` / `strict_privacy` 开关 |
| **B** | 3h | 隐私控制面板 | `graphcampus/frontend/pages/07_privacy.py` | 隐私模式切换（离线/标准/严格）+ 数据流向信息卡 |
| **C** | 3h | 隐私降级测试 | `graphcampus/tests/test_utils/test_privacy.py` | 三种隐私模式降级验证 |

---

#### Day 27

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 配置管理 + 特征开关 API | `graphcampus/utils/config_loader.py` | 完善：运行时热加载 API |
| | | | 修改 `graphcampus/utils/health.py` | 配置变更后的健康检查 |
| **B** | 3h | 系统管理前端 | `graphcampus/frontend/pages/08_admin.py` | 配置查看/编辑 + 特征开关切换界面 |
| **C** | 3h | 文档初稿 | `graphcampus/docs/api_documentation.md` | API 文档 |
| | | | `graphcampus/docs/configuration_guide.md` | 配置说明 |

---

#### Day 28 — 完整联调

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 四 Agent 全流程贯通 | 全面联调：冷启动 → 选课 → 双路验证 → 图谱 → 路径 → PPR → RAG → 百事通 → 课后复习 | |
| | | | 修复所有阻塞问题 | |
| **B** | 3h | UI 统一打磨 | UI 统一配色/间距/字体 + 十万个为什么体验优化 | |
| | | | 空状态/加载态/错误态/边界态覆盖 | |
| **C** | 3h | 全量回归测试 | `pytest --cov=./ --cov-report=html` 全量回归 + 覆盖率统计 | |

**周末 Review：** 四 Agent 全流程演示，检查覆盖率 > 60%。

---

### 📅 第 5 周：工业化 + 边界测试 + 数据完善

---

#### Day 29

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 测试框架完善 | `graphcampus/tests/conftest.py` | 完善全局夹具：Mock LLM、内存 SQLite、测试 Embedding |
| | | | `graphcampus/tests/test_models.py` | 所有 Pydantic model 单元测试 |
| | | | `graphcampus/tests/test_agents/test_planner.py` | 规划师子图测试 |
| **B** | 3h | Docker 构建 | `graphcampus/docker/Dockerfile` | 多阶段构建 |
| | | | `graphcampus/docker/.dockerignore` | |
| | | | 本地 Docker 构建测试 | |
| **C** | 3h | 边界测试 | `graphcampus/tests/test_utils/test_llm_client.py` | LLM 超时/空响应/异常测试 |
| | | | `graphcampus/tests/test_rag/test_hybrid.py` | 补充：空结果、低分数边界 |

---

#### Day 30

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | LLM 调用层边界 | 修改 `graphcampus/utils/llm_client.py` | 边界处理：超时 → 重试 → 熔断 → 降级规则回复 |
| | | | 修改 `graphcampus/utils/rate_limiter.py` | 完善：白名单机制 |
| **B** | 3h | 兜底交互对齐 | 所有前端页面：错误提示统一 + 降级提示美化 | |
| | | | 各页面兜底文案（E001-E008） | |
| **C** | 3h | 速率限制测试 | `graphcampus/tests/test_utils/test_rate_limiter.py` | 速率限制 + 熔断测试 |

---

#### Day 31

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | RAG 质量调优 | 调整 chunk_size / Top-K / RRF k 参数 | |
| | | | A/B 测试不同分块策略的检索效果 | |
| **B** | 3h | 图谱交互增强 | 修改 `graphcampus/frontend/components/graph_viz.py` | 搜索框、节点高亮、缩放控制、详情浮窗 |
| **C** | 3h | 数据质量报告 | `graphcampus/scripts/check_data_quality.py` | 完善：全量数据质量报告 |
| | | | `graphcampus/data/data_quality_report.json` | 索引优化建议 |

---

#### Day 32

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 性能测试 | `graphcampus/tests/benchmarks/test_ppr_perf.py` | PPR 40 节点性能测试 |
| | | | `graphcampus/tests/benchmarks/test_node2vec_perf.py` | Node2Vec 性能测试 |
| | | |性能优化（如有必要） | |
| **B** | 3h | UI 统一 | 统一配色方案、间距、字体、移动端适配 | |
| **C** | 3h | 覆盖率冲刺 | 补充测试使覆盖率 > 80% | |
| | | | `graphcampus/tests/test_coverage.txt` | 覆盖率报告 |

---

#### Day 33 — ⭐ 十万个为什么质量调优

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 十万个为什么质量调优 | 分块策略调优（chunk_size / overlap / 标题权重） | |
| | | | 路由准确率评估 + 调优 | |
| | | | 时间衰减系数 λ 调优 | |
| **B** | 3h | 十万个为什么细节打磨 | 空状态："暂未收录该问题，换个问法试试" | |
| | | | 加载态：骨架屏 | |
| | | | 错误态：友好错误提示 | |
| | | | 边界态：超长问题 / 空输入 / 特殊字符 | |
| **C** | 3h | 数据补充 | `graphcampus/data/raw/` | 继续收集更多校园数据 |
| | | | 补充遗漏的常见问题数据 | |

---

#### Day 34

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | Docker + CI 完善 | 修改 `graphcampus/docker/Dockerfile` | 最终优化：依赖缓存、多阶段、镜像 < 500MB |
| | | | `graphcampus/.github/workflows/ci.yml` | 完整 CI：lint + test + cov + build + 数据质量 |
| **B** | 3h | Docker + CI 联调 | 本地跑通完整 CI 流程 | |
| | | | 修复 CI 中发现的问题 | |
| **C** | 3h | 最终数据质量 | `graphcampus/scripts/check_data_quality.py` | 最终数据质量验证 |
| | | | 索引完整性检查 | |

---

#### Day 35 — 最终 Bug 清理

| 角色 | 时长 | 任务 | 产出文件 | 关键代码 |
|------|------|------|---------|---------|
| **A** | 8-10h | 全场景回归 | 回归所有功能 + 边界场景 | |
| | | | 修复所有已知 Bug | |
| **B** | 3h | 演示准备 | `graphcampus/presentation/demo_script.md` | 操作剧本 |
| | | | `graphcampus/presentation/demo_data_prep.py` | 演示数据准备 |
| **C** | 3h | 最终测试报告 | `graphcampus/tests/test_report_final.md` | 最终测试报告（覆盖率、通过率、已知问题） |
| | | | `pytest --cov=./ --cov-report=html` 最终覆盖率报告 | |

**周末 Review：** 项目功能冻结，准备答辩。

---

### 📅 第 6 周（可选/缓冲）：文档 + 答辩

---

#### Day 36

| 角色 | 时长 | 任务 | 产出文件 |
|------|------|------|---------|
| **A** | 8-10h | 互相黑盒测试 | 测试 B/C 负责的模块，报告 Bug |
| | | 修 Bug | |
| **B** | 3h | 演示视频录制 | `graphcampus/presentation/demo_video.mp4`（5 分钟） |
| **C** | 3h | 互相黑盒测试 | 测试 A/B 负责的模块，报告 Bug |

---

#### Day 37

| 角色 | 时长 | 任务 | 产出文件 |
|------|------|------|---------|
| **A** | 8-10h | 技术白皮书 | `graphcampus/presentation/technical_whitepaper.md` → 导出 PDF |
| | | 架构图 / 算法说明 / 工业化设计 / 性能数据 | |
| **B** | 3h | PPT 制作 | `graphcampus/presentation/答辩PPT.pptx` |
| | | 演示彩排 | |
| **C** | 3h | 技术白皮书协作 | 协助 A 写性能数据和质量报告部分 |
| | | README 最终完善 | `graphcampus/README.md` |

---

#### Day 38

| 角色 | 时长 | 任务 | 产出文件 |
|------|------|------|---------|
| **A** | 2h | 模拟答辩（主讲算法/工业化） | — |
| **B** | 2h | 模拟答辩（主讲操作演示） | — |
| **C** | 2h | 模拟答辩（辅助 Q&A） | — |
| 全体 | 复盘 | 模拟答辩复盘 + 改进 | — |

---

#### Day 39

| 角色 | 时长 | 任务 | 产出文件 |
|------|------|------|---------|
| **A** | 第二轮模拟答辩 + 提交准备 | `graphcampus/presentation/` 最终检查 |
| **B** | 视频最终剪辑 | `graphcampus/presentation/demo_video.mp4` |
| **C** | 所有文档归档 | 确认所有文档齐全 |

---

#### Day 40 — 提交

| 角色 | 时长 | 任务 |
|------|------|------|
| **A+B+C** | 全天 | 最终提交：代码 + 数据 + 文档 + 视频 + PPT |

---

## 三、每日必做事项

### 3.1 每日站会（10 分钟）

- 时间：每天固定时间（建议上午）
- 主持人：A
- 议程：
  1. 昨天完成了什么？（每人 2 分钟）
  2. 今天要做什么？（每人 1 分钟）
  3. 有什么阻塞？（2 分钟）

### 3.2 每日 Commit 规范

```bash
git checkout -b feat/dayN-xxx    # 新建分支
# ... coding ...
git add <files>
git commit -m "Day N: 做了什么"   # 中文描述
git push origin feat/dayN-xxx
# 创建 PR → 至少 1 人 review → merge 到 main
```

### 3.3 每日完成检查清单

- [ ] 当天分配的代码文件已创建/修改？
- [ ] 代码已运行无报错？
- [ ] 新增代码已写对应测试？
- [ ] 本地 `pytest` 通过？
- [ ] 已 push 到 GitHub？

---

## 四、附录：快速参考

### 4.1 各周核心交付物速查

| 周次 | 核心交付 | 验收标准 |
|------|---------|---------|
| W1 | 项目骨架 + 配置 + 数据管道 | 启动不报错，数据库建表成功，PDF 解析可跑通 |
| W2 | 双路验证 + PPR + 百事通基础 | 课程录入→图谱→百事通分类路由可跑通 |
| W3 | Hybrid RAG + 语义缓存 + 百事通完整 | RAG 问答可对话，百事通三类数据可检索，缓存命中 |
| W4 | 课后复习 + 并行 + 隐私 + 工业化 | 四 Agent 全流程贯通，隐私模式切换正常 |
| W5 | 工业化 + 边界测试 + 数据完善 | 测试覆盖率 > 80%，Docker 构建成功，CI 通过 |
| W6 | 文档 + 答辩 | 白皮书、PPT、视频齐备，模拟答辩流畅 |

### 4.2 常用命令

```bash
# 启动应用
streamlit run main.py

# 运行测试
pytest
pytest --cov=./ --cov-report=html
pytest tests/test_campus_qa/ -v

# 数据质量检查
python -m scripts.check_data_quality

# 构建索引
python -m scripts.build_indexes

# Docker 构建
docker build -t graphcampus -f docker/Dockerfile .
```

### 4.3 关键参数默认值

| 参数 | 默认值 | 配置文件路径 |
|------|--------|------------|
| LLM model | gpt-4o-mini | `config/config.yaml` → llm.model |
| chunk_size | 512 | `config/config.yaml` → rag.chunk_size |
| chunk_overlap | 50 | `config/config.yaml` → rag.chunk_overlap |
| RRF k | 60 | `config/config.yaml` → rag.rrf_k |
| 语义缓存阈值 | 0.92 | `config/config.yaml` → cache.semantic_similarity_threshold |
| 缓存 TTL | 3600s | `config/config.yaml` → cache.ttl_seconds |
| 时间衰减 λ | 0.01 | `config/config.yaml` → campus_qa.time_decay_lambda |
| 速率限制 | 10 req/min/user | `config/config.yaml` → llm.rate_limit_per_min |
| 熔断阈值 | 5 次失败 | `config/config.yaml` → llm.circuit_breaker_threshold |

---

> 本文档为 `GraphCampus_项目规划书.md` 的每日执行配套文档。
> 每日站会时对照检查进度，如有偏离及时调整。
> 最后更新：2026 年 6 月
