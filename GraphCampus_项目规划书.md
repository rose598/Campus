# GraphCampus 项目规划书

> 基于状态图的多智能体校园助手 —— 三人协同开发版（五邑大学定制）
> 团队：A（8-10h/天）+ B（3h/天）+ C（3h/天）· 周期：5-6 周

---

## 目录

1. [项目概述](#1-项目概述)
2. [数据来源](#2-数据来源)
3. [团队分工](#3-团队分工)
4. [技术架构](#4-技术架构)
5. [核心功能](#5-核心功能)
6. [冷启动引导](#6-冷启动引导新用户首次体验)
7. [工业化设计](#7-工业化设计)
8. [兜底设计](#8-兜底设计失败路径处理)
9. [隐私设计](#9-隐私设计)
10. [未来展望](#10-未来展望不作本次开发承诺)
11. [5-6 周协同开发计划](#11-周协同开发计划三人版-30-36天)
12. [交付物清单](#12-交付物清单)
13. [协作规范](#13-协作规范)
14. [答辩策略](#14-答辩策略)

---

## 1. 项目概述

### 1.1 一句话概括

**GraphCampus** 是一个基于 LangGraph 状态图的四智能体校园助手——由"规划师""情报官""学伴""百事通"四个 AI Agent 组成。它能自动解析专业课大纲、梳理课程先修依赖、推荐最优学习路径、课后关联笔记到知识体系，并新增**校园十万个为什么**功能：基于海量校园非结构化数据（教务处通知、生活指南、课程资料），让每个新生都能像问学长一样——"快递站在哪？""补考什么时候报名？""这门课期末考什么？"——秒级获得带来源引用的准确回答。

### 1.2 解决的问题

| 场景 | 痛点 |
|------|------|
| 选课迷茫 | 只看到课程名，不知道具体讲什么、难不难、需要什么基础 |
| 先修关系不清 | 选了某门课才发现跟不上 |
| 学完不知道下一步 | 不知道后续该学什么 |
| 想了解课程细节 | 想问问题但没人及时回答 |
| 课程与资源脱节 | 不知道这门课对应什么研究方向、实验室机会 |
| 信息茧房 | 讲座/竞赛存在但自己不知道 |
| **校园生活困惑** | **快递点在哪？校医院怎么报销？哪个食堂窗口好吃？——问学长没人理，翻公众号找不到** |
| **教务信息淹没** | **选课通知、补考安排、转专业政策散落在不同网页和 PDF 里，搜不到** |
| **期末复习迷茫** | **往年考什么？重点在哪？课程资料分散在各处** |

### 1.3 本校化定位

- 仅服务 **本校**，数据来源于本校培养方案、课程大纲、教务处官网、校园公众号
- 知识图谱预置本校教师、课程、实验室、研究方向
- 校园十万个为什么数据全部来源于本校真实渠道（教务处通知、校园生活指南、课程资料）
- 开发与演示均使用本校真实数据

---

## 2. 数据来源

### 2.1 结构化数据（原系统）

| 数据 | 来源 | 获取方式 |
|------|------|---------|
| 专业课列表 | 教务处官网 / 培养方案 PDF | 手动下载 → 结构化 |
| 课程大纲 | 学院网站 / 教师提供 | LLM 辅助提取 + 模板填空 |
| 先修关系 | 课程大纲 + Node2Vec 预测 | 双路 LLM 交叉验证 + 链路预测补全 |
| 授课教师 | 教务处 / 课程大纲 | 与课程一起录入 |
| 实验室 / 研究方向 | 学院官网、教师主页 | BeautifulSoup 爬取 |
| 讲座 / 竞赛 | 官网 / 公众号 | 预置 JSON（主）+ 爬虫（辅） |
| 课堂笔记 | 学生上传 | 文本粘贴 → LLM + Embedding |
| 已修课程 | 学生手动标记 | 界面勾选 |

### 2.2 非结构化数据（校园十万个为什么）

| 数据类别 | 来源 | 获取方式 | 预估数据量 | 更新频率 |
|----------|------|---------|-----------|---------|
| **① 教务处历年通知** | 教务处官网、教务管理系统 | 爬虫（BeautifulSoup）+ PDF 解析（pypdfium2）+ 历史归档 | 200-500 篇 | 每周增量 |
| - 选课通知（每学期初） | 教务处 → 选课专栏 | 增量爬取 | 10-20 篇/学期 | 学期更新 |
| - 补考/缓考安排 | 教务处 → 考务专栏 | 增量爬取 | 5-10 篇/学期 | 学期更新 |
| - 转专业政策 | 教务处 → 学籍管理 | 手动下载 PDF | 3-5 篇 | 年更新 |
| - 四六级/普通话报名 | 教务处 → 考试专栏 | 增量爬取 | 5-10 篇/年 | 年更新 |
| - 休学/复学/退学流程 | 教务处 → 学籍管理 | 手动整理 | 5-10 篇 | 年更新 |
| **② 校园生活指南** | 公众号、校内论坛、后勤官网 | 爬虫 + 手动整理 + LLM 清洗 | 100-300 篇 | 按需更新 |
| - 快递点位置/营业时间 | 后勤官网、公众号 | 爬虫 | 5-10 篇 | 学期更新 |
| - 食堂窗口推荐/价格 | 公众号、学生投稿 | 手动整理 | 20-50 篇 | 按需 |
| - 校医院就诊/报销流程 | 后勤官网 | 手动整理 | 5-10 篇 | 年更新 |
| - 图书馆借阅规则/开放时间 | 图书馆官网 | 爬虫 | 5-10 篇 | 年更新 |
| - 校园卡充值/挂失 | 信息化中心 | 手动整理 | 3-5 篇 | 年更新 |
| - 宿舍报修/调宿流程 | 后勤官网 | 手动整理 | 5-10 篇 | 年更新 |
| - 校内交通车时刻表 | 后勤官网 | 爬虫 | 3-5 篇 | 学期更新 |
| **③ 课程资料与期末复习** | 学院网站、教师提供、学生贡献 | LLM 辅助提取 + 学生上传 | 每门课 10-50 份 | 学期更新 |
| - 课程大纲（详细版） | 学院网站、教师 | PDF 解析 | 每门课 1 份 | 学期更新 |
| - 教学日历 | 教师提供 | PDF 解析 | 每门课 1 份 | 学期更新 |
| - 课件 PPT/PDF | 教师提供 / 学生上传 | 上传解析 | 每门课 5-20 份 | 学期更新 |
| - 往年期末试题 | 学生上传 / 收集 | 上传解析 | 每门课 1-5 份 | 累积 |
| - 复习资料/重点归纳 | 学生上传 | 文本粘贴 → LLM 清洗 | 每门课 1-5 份 | 学期更新 |

### 2.3 非结构化数据处理管道

```
原始文档 (PDF/Word/HTML)
    │
    ▼
文档解析层 (pypdfium2 + python-docx + BeautifulSoup)
    │
    ▼
文本清洗 (去噪、标准化、去重)
    │
    ▼
智能分块 (Layout-aware chunking)
    ├── 按标题层级切分 (Markdown header)
    ├── 语义完整性约束 (段落不截断)
    └── 重叠窗口 (overlap=50 tokens)
    │
    ▼
元数据标注 (LLM 辅助)
    ├── 数据类别 (教务/生活/课程)
    ├── 发布时间 / 有效期
    ├── 关键词标签
    └── 来源 URL
    │
    ▼
双路索引
    ├── BM25 倒排索引
    └── Dense Embedding 索引
    │
    ▼
写入 SQLite (WAL 模式)
    ├── documents 表 (原始文本 + 元数据)
    ├── chunks 表 (分块文本 + 元数据继承)
    └── embeddings 表 (chunk_id + vector)
```

---

## 3. 团队分工

### 3.1 角色总览

| 角色 | 每天可用时间 | 核心定位 | 主要精力分配 |
|------|------------|---------|------------|
| **A** | 8-10 小时 | **架构 + 核心算法 + 工业化基建** — 项目总负责人 | 60% 核心算法 + 25% 工业化基建 + 15% 联调兜底 |
| **B** | 3 小时 | **前端开发 + 数据采集** — 交互与可视化 | 60% Streamlit 前端 + 30% 爬虫数据采集 + 10% 演示 |
| **C** | 3 小时 | **数据处理 + 测试 + 文档** — 质量保障 | 40% 非结构化数据处理 + 35% 测试 + 25% 文档/演示 |

### 3.2 角色 A：架构 + 核心算法 + 工业化基建（8-10h/天）

| 职责 | 具体内容 | 优先级 |
|------|---------|--------|
| LangGraph 工作流 | 父图 + 四子图（新增百事通子图）、并行节点、中断点、异步编排 | P0 |
| 双路交叉验证 | 两个独立 LLM 并行提取 → Jaccard 比对 → 置信度分级决策 | P0 |
| Node2Vec 链路预测 | 课程节点嵌入 → 预测缺失先修关系 → 推荐确认 | P0 |
| Hybrid RAG 引擎 | BM25 + Dense Embedding 混合检索 + RRF 融合 + Query Rewriting | P0 |
| **校园十万个为什么引擎** | **非结构化数据处理管道核心、分类路由、时间感知排序、多源 RAG** | **P0** |
| PPR 推荐 | 定制 Personalized PageRank 实现 | P1 |
| 语义缓存 | LLM 调用结果缓存（余弦相似度 + TTL + LRU） | P0 |
| **工业化基建** | **配置中心、特征开关、健康检查、速率限制、错误码体系** | **P0** |
| 结构化日志 + 性能追踪 | structlog + 自建 tracer，各节点耗时打点 | P0 |
| LLM 提示词工程 | 10 套提示词模板（含新增"校园问答"模板） | P1 |
| 测试框架搭建 | pytest 配置、Mock LLM 夹具、覆盖率配置 | P1 |
| Docker + CI 架构 | 多阶段构建、GitHub Actions 流程设计 | P1 |

### 3.3 角色 B：前端开发 + 数据采集（3h/天）

| 职责 | 具体内容 | 优先级 |
|------|---------|--------|
| Streamlit 前端骨架 | 路由、组件库、状态同步、统一布局 | P0 |
| **校园十万个为什么前端** | **问答界面、分类 Tab（教务/生活/课程）、分类展示、来源引用卡片** | **P0** |
| 图谱可视化 | Pyvis 课程关系图（搜索 / 高亮 / 缩放） | P0 |
| RAG 问答界面 | 对话 UI + BM25 / 向量双栏展示 + 引用标注 | P0 |
| 课程详情页 | 信息卡 + 先修链 + 后继课程 + 相关教授 + 资料下载 | P0 |
| 冷启动引导 | 选专业 → 课程列表 → 标记已修 → 图谱渲染 | P0 |
| 异步任务 UI | LLM 调用进度条 + 状态通知 | P1 |
| 情报官推送面板 | PPR 推荐结果展示 + 推理链溯源 | P1 |
| 课后复习界面 | 笔记粘贴/上传 + 图谱高亮 + 复习建议卡 | P1 |
| 隐私控制面板 | 隐私模式切换 + 数据流向信息卡 | P1 |
| **数据采集** | **BeautifulSoup 爬虫（教务处通知、生活指南）、PDF 批量下载** | **P1** |
| DevOps 配合 | Docker 镜像构建、CI 配合 | P2 |
| 演示准备 | 剧本、PPT、视频录制 | P2 |

### 3.4 角色 C：数据处理 + 测试 + 文档（3h/天）

| 职责 | 具体内容 | 优先级 |
|------|---------|--------|
| **非结构化数据处理** | **数据采集辅助、PDF 解析、文本清洗、分块、元数据标注、质量评估** | **P0** |
| **数据质量保障** | **分块质量检查、Embedding 质量验证、检索质量评估（MRR/Recall）** | **P0** |
| **测试 - 单元测试** | Pydantic model 测试、Node 函数测试、工具函数测试 | P0 |
| **测试 - 集成测试** | 子图全流程测试（Mock LLM）、十万个为什么管道测试 | P0 |
| 测试 - 端到端 | 用户操作路径测试 | P1 |
| 测试 - 边界测试 | LLM 超时/空响应/异常、RAG 检索不到、空数据 | P1 |
| 测试覆盖率跟踪 | pytest-cov 配置、>80% 目标跟踪 | P1 |
| **课程数据录入** | **预置一个完整专业（~40 门课）+ 双路验证标注** | P1 |
| **校园数据整理** | **教务处通知整理、生活指南整理、课程资料收集** | P1 |
| 文档 - 技术白皮书 | 架构图 / 算法说明 / 工业化设计 / 性能数据 | P2 |
| 文档 - README 维护 | 接口定义、环境配置、快速开始 | P2 |
| 答辩辅助 | 演示数据准备、Q&A 整理 | P2 |

### 3.5 公共模块（A 主导 + B/C 配合）

| 模块 | 负责人 | 配合 |
|------|--------|------|
| AgentState 定义 + JSON Schema（Day 1 锁死） | A | B/C review |
| LLM 中枢封装（重试 / 熔断 / 缓存 / 并发控制） | A | C 测试 |
| Embedding 服务封装 | A | B 前端配合 |
| 结构化日志接口（统一 trace_id） | A | C 文档 |
| Pydantic model 层 | A | C 测试 |
| 配置中心（YAML + .env + 运行时热加载） | A | B 前端配置页面 |
| 特征开关（Feature Flag） | A | B/C 配合 |
| 健康检查端点 | A | B 前端状态页 |

---

## 4. 技术架构

### 4.1 架构总览

```
┌──────────────────────────────────────────────────────────────────────┐
│                         Streamlit 前端                                │
│  课程地图  课程详情  校园十万个为什么  RAG问答  推送面板  课后复习    │
│                   ┌──────────────────────┐                          │
│                   │ 校园十万个为什么      │                          │
│                   │ [教务通知] [生活指南] [课程资料]   ← 分类 Tab   │
│                   │ 问答输入框 + 来源引用卡片 + 时间标签            │
│                   └──────────────────────┘                          │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
┌──────────────────────────┴───────────────────────────────────────────┐
│                       LangGraph 工作流引擎                            │
│                                                                      │
│   ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│   │ 规划师子图   │  │ 情报官子图  │  │ 学伴子图    │  │ 百事通子图  │   │
│   │ ·双路验证    │  │ ·PPR推荐   │  │ ·Hybrid    │  │ ·分类路由   │   │
│   │ ·Node2Vec   │  │ ·路径溯源   │  │ ·RAG       │  │ ·时间排序   │   │
│   │ ·拓扑排序    │  │            │  │ ·Query     │  │ ·多源RAG   │   │
│   │ ·课后复习    │  │            │  │  Rewrite   │  │ ·来源引用   │   │
│   │             │  │            │  │ ·语义缓存   │  │ ·元数据过滤 │   │
│   │             │  │            │  │ ·共情生成   │  │            │   │
│   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘ │
│          └────────────────┼────────────────┼────────────────┘        │
│                           │              │                           │
│                    ┌──────┴──────────────┴──────┐                    │
│                    │        AgentState           │                    │
│                    │    滑动窗口 + 摘要 + 分类索引 │                    │
│                    └──────────────┬──────────────┘                    │
└───────────────────────────────────┼──────────────────────────────────┘
                                    │
┌───────────────────────────────────┼──────────────────────────────────┐
│                                   │                                  │
│  ┌────────────────────────────────┴────────────────────────┐        │
│  │              LLM + Embedding 服务层                       │        │
│  │  OpenAI / 国产大模型 + sentence-transformers              │        │
│  │  重试 (3次, 指数退避)   熔断 (5错误→停30s)                 │        │
│  │  语义缓存 (Sim>0.92 命中, TTL=3600s)  并发控制 (max=3)    │        │
│  │  速率限制 (10 req/min/user)   特征开关 (随时关闭某功能)    │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                    │                                  │
│  ┌───────┐ ┌───────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌─────────┐       │
│  │SQLite │ │NetworkX│ │BM25  │ │ 日志  │ │ 缓存  │ │ 配置中心 │       │
│  │ 数据   │ │ 图谱    │ │ 倒排  │ │(结构  │ │(语义  │ │(YAML +  │       │
│  │ +向量  │ │ +PPR   │ │ 索引  │ │ 化)   │ │ 桶)   │ │ 热加载) │       │
│  │ WAL   │ │ +NVec  │ │      │ │trace │ │TTL   │ │ Feature │       │
│  └───────┘ └──────┘ └──────┘ └──────┘ └──────┘ │ Flag   │       │
│  ┌───────────────────────────────────────────┘ └─────────┘       │
│  │  校园非结构化数据索引                                          │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐                    │
│  │  │ 教务索引  │  │ 生活索引  │  │ 课程索引  │                    │
│  │  │ BM25+Emb │  │ BM25+Emb │  │ BM25+Emb │                    │
│  │  └──────────┘  └──────────┘  └──────────┘                    │
│  └──────────────────────────────────────────────────────────────┘
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│  工业化基础设施                                                       │
│  Pydantic校验 │ 配置中心 │ 特征开关 │ 健康检查 │ 速率限制 │          │
│  异步Task队列 │ CI/CD │ Docker │ 结构化日志 │ 错误码体系 │           │
│  单元测试(>80%) │ 集成测试 │ E2E测试 │ 数据质量门禁                  │
└──────────────────────────────────────────────────────────────────────┘
```

### 4.2 技术栈选型

| 层次 | 选型 | 说明 |
|------|------|------|
| 前端 | **Streamlit** | 快速迭代，一人可维护 |
| 工作流 | **LangGraph** | 并行 / 中断 / 异步 / 子图嵌套 |
| LLM | **OpenAI / 国产大模型** | 调用层：重试 + 熔断 + 缓存 + 并发控制 + 速率限制 |
| Embedding | **sentence-transformers** | 本地运行，零 API 成本 |
| 混合检索 | **BM25（rank_bm25）+ Dense（余弦）** | RRF 融合，支持多索引路由 |
| 文档解析 | **pypdfium2 + python-docx + BeautifulSoup** | PDF / Word / HTML 三端覆盖 |
| 智能分块 | **自建 Layout-aware chunker** | 按标题层级 + 语义完整性 + 重叠窗口 |
| Query Rewriting | **LLM 改写** | 多轮对话上下文压缩 |
| 知识图谱 | **NetworkX** | PPR / Node2Vec / 拓扑排序 |
| Node2Vec | **Node2Vec 库** | 链路预测发现缺失先修 |
| 语义缓存 | **自建（余弦相似度 + TTL + LRU）** | 相同/相似请求命中缓存 |
| 数据库 | **SQLite（WAL 模式）** | 高并发读取，零运维 |
| 配置中心 | **自建（YAML + .env + 运行时热加载）** | 统一配置管理 |
| 特征开关 | **自建 Feature Flag** | 按功能/用户/环境灰度 |
| 校验 | **Pydantic v2** | 所有 I/O 运行时校验 |
| 日志 | **structlog + 自建 tracer** | JSON 格式 + trace_id + 节点耗时 |
| 测试 | **pytest + pytest-cov** | 单元测试 > 80% 覆盖率 |
| CI | **GitHub Actions** | PR 自动 lint + test + build + 数据质量检查 |
| 容器 | **Docker** | 多阶段构建，镜像 < 500MB |

### 4.3 关键技术算法

#### 4.3.1 双路交叉验证 + 置信度分级

```
输入: 课程大纲文本

LLM-A 提取先修          LLM-B 提取先修
   │                       │
   └────────┬──────────────┘
            │
     Jaccard 相似度计算
            │
    ┌───────┼───────┐
    │       │       │
  =1.0   0.5-0.99  <0.5
    │       │       │
  自动写入  中断确认  强制手动
  图谱     争议项    编辑
```

- **高置信度**（J=1.0）：自动写入图谱，记录 `confidence: 1.0`
- **中置信度**（J=0.5~0.99）：中断展示两路差异项，用户勾选确认
- **低置信度**（J<0.5）：完整中断，表单手动编辑
- 幻觉过滤叠加：课程名不在预置列表中 → 打回；检测到循环依赖 → 打回

#### 4.3.2 Node2Vec 链路预测（发现缺失先修）

对于已录入的课程图谱，用 Node2Vec 学习每个课程节点的向量表示，然后：

1. 对每对未直接连接的课程 (u, v)，计算其在嵌入空间中的余弦相似度
2. 相似度高于阈值（如 0.85）且符合学期逻辑（先修课早于后继课）→ 作为"潜在先修关系"推荐给用户
3. 用户确认后加入图谱

**价值**：教师可能漏写了先修关系，算法自动补全，答辩时可以展示"系统发现了一个你可能没注意到的先修关系"。

#### 4.3.3 Hybrid RAG（BM25 + Dense + RRF）

学生提问 → Query Rewriting（多轮）→ 同时检索两条路：

```
用户问题
   │
 Query Rewriting (LLM 改写)
   │
   ├──→ BM25 稀疏检索 (关键词匹配)
   │       │
   │       └──→ Top-5
   │
   └──→ Dense 稠密检索 (语义匹配)
           │
           └──→ Top-5
                   │
            Reciprocal Rank Fusion (RRF)
                   │
              Top-3 → LLM 生成回答
```

- **Query Rewriting**：多轮对话时，LLM 将历史上下文压缩到当前问题中
- **BM25**：关键词精确匹配
- **Dense**：语义匹配
- **RRF**：`score = Σ 1/(k + rank)` 融合两个排序，k=60

#### 4.3.4 校园十万个为什么：分类路由 + 多源 RAG

这是本次新增的核心算法。其核心创新在于**按数据类别路由 + 时间感知排序 + 多索引并行检索**。

```
用户提问: "补考什么时候报名？"
   │
   ▼
意图分类 (LLM 轻量分类)
   ├── 教务类（选课/补考/转专业...）→ 路由到教务索引
   ├── 生活类（快递/食堂/校医院...）→ 路由到生活索引
   ├── 课程类（大纲/复习/课件...）→ 路由到课程索引
   └── 通用类（不确定）→ 全部检索 + 分数加权
   │
   ▼
并行检索 (按目标索引)
   ├── 教务索引: BM25 + Dense → RRF
   │   └── 时间衰减加权 (近期通知权重更高)
   ├── 生活索引: BM25 + Dense → RRF
   │   └── 静态排序 (常问问题优先)
   └── 课程索引: BM25 + Dense → RRF
       └── 课程关联过滤 (仅匹配用户已修/关注课程)
   │
   ▼
多源融合
   ├── 分数归一化 (Min-Max)
   ├── 跨源 RRF (如有多个分类命中)
   └── 元数据标注 (来源类别 + 发布时间 + 有效期)
   │
   ▼
LLM 生成
   ├── 引用标注 [来源: 教务处通知 2026-02-20]
   ├── 时间提示 (如通知已过期提示"仅供参考")
   └── 置信度标签 (高/中/低)
   │
   ▼
输出: "根据教务处 2026 年 2 月 20 日通知，
       补考报名时间为 2026 年 3 月 1 日-5 日，
       登录教务系统 → 考试报名 → 补考确认。
       [来源: 教务处 → 考务专栏 → 2025-2026-2 补考通知]"
```

**关键技术点**：

| 技术 | 实现 | 价值 |
|------|------|------|
| 意图分类 | LLM 轻量分类（3-5 类）或 Embedding + KNN | 准确路由到对应索引，减少噪声 |
| 时间衰减加权 | `score = base_score × exp(-λ × days_ago)`，λ=0.01 | 过期通知降权，最新通知优先 |
| 有效期过滤 | metadata.expiry 字段，到期自动排除 | 避免已过时政策误导 |
| 多源 RRF | 跨类别结果统一排序 | 模糊问题也能召回 |
| 来源引用卡片 | 标题 + 日期 + URL + 原文片段 | 可信、可追溯 |

#### 4.3.5 智能分块策略（Layout-aware Chunking）

针对校园非结构化数据的特点，采用层次化分块策略：

```
原始文档
    │
    ▼
标题检测 (Markdown/HTML heading / PDF 标题)
    │
    ▼
按标题层级切分
    ├── H1 → 文档级
    ├── H2 → 章节级 (chunk_size=512, overlap=50)
    └── H3+ → 子章节级 (可合并到 H2)
    │
    ▼
语义完整性约束
    ├── 段落不截断 (以换行符为边界)
    ├── 表格整体保留
    └── 列表整体保留
    │
    ▼
元数据继承
    ├── 父级标题链 (如: "教务处通知 > 选课通知 > 时间安排")
    ├── 文档级元数据 (来源、日期、类别)
    └── 位置编号 (用于引用溯源)
```

#### 4.3.6 Personalized PageRank

在包含课程 / 教师 / 实验室 / 研究方向 / 讲座 五类节点的异构图上运行。personalization vector 以用户当前关注的课程或研究方向为中心分配权重。迭代收敛后，取分数最高的推荐机会。

#### 4.3.7 语义缓存

- **Key**：`(system_prompt_hash, user_message_embedding)`
- **命中条件**：余弦相似度 > 0.92
- **TTL**：3600s（1 小时），LRU 淘汰
- **存储**：SQLite 缓存表
- **新增校园场景复用**：同一宿舍不同学生问"快递站在哪？"→ 第二次直接命中缓存，零延迟

### 4.4 LangGraph 状态图设计

```
ParentGraph
│
├── Input: 课程大纲 / 笔记 / 问答 / 已修标记 / 校园问题
│
├── Node 1: 解析节点 (Parse)
│   ├── SubNode: 双路 LLM 提取 (LLM-A + LLM-B)
│   ├── SubNode: 交叉验证 (Jaccard + 置信度)
│   │    ├── 高 → 直接入库
│   │    ├── 中 → [interrupt_before] 用户确认
│   │    └── 低 → [interrupt_before] 手动编辑
│   ├── SubNode: Node2Vec 链路预测 (推荐缺失先修 → 用户确认)
│   ├── SubNode: 幻觉过滤 (名校验 + 循环依赖)
│   └── SubNode: 笔记→Embedding (课后复习场景)
│
├── Node 2: 意图路由 (新增)
│   ├── → 课程相关: 规划师 + 情报官 + 学伴
│   └── → 校园问题: 百事通
│
├── Node 3: Fan-out 并行调度
│   ├── → 规划师 (图谱 / 路径 / 复习)
│   ├── → 情报官 (PPR / 推送)
│   ├── → 学伴 (RAG / 共情)
│   └── → 百事通 (校园十万个为什么)
│
├── Node 4: 各子图执行 (并行)
│   │
│   ├── Agent_1: 规划师
│   │   ├── 图谱更新 + Node2Vec 增量训练
│   │   ├── 学习路径 (拓扑 → 学期约束 → 备用)
│   │   └── 课后复习 (知识点→图谱标注)
│   │
│   ├── Agent_2: 情报官
│   │   ├── PPR 初始化 (personalization)
│   │   ├── PPR 迭代 → Top-K
│   │   └── 推理链生成 (路径溯源)
│   │
│   ├── Agent_3: 学伴
│   │   ├── Query Rewriting (多轮)
│   │   ├── Hybrid RAG (BM25+Dense → RRF → LLM)
│   │   ├── 语义缓存查/写
│   │   └── LLM 共情生成
│   │
│   └── Agent_4: 百事通 (新增)
│       ├── 意图分类 (教务/生活/课程)
│       ├── 分类路由 → 对应索引
│       ├── 时间感知排序 + 有效期过滤
│       ├── 多源融合 + RRF
│       ├── 语义缓存查/写
│       └── LLM 生成 (含来源引用 + 时间标签)
│
├── Node 5: Fan-in 聚合
│   └── 合并 → 响应
│
└── Output: 响应 + 结构化日志 (含各节点耗时 + 分类路由信息)

AgentState:
{
  "user_id": str,
  "messages": List[BaseMessage],         // 滑动窗口 20 轮
  "chat_summary": str,                   // LLM 摘要
  "courses": Dict[str, Course],
  "course_graph": Graph,                 // NetworkX 图
  "node2vec_model": Optional[Model],     // Node2Vec 模型 (增量更新)
  "bm25_index": BM25Index,               // 所有 BM25 索引 (含分类索引)
  "dense_index": Dict[str, List[float]], // {chunk_id: embedding}
  "cache_pool": Dict[str, str],          // 语义缓存
  "user_progress": Dict,
  "learning_path": Optional[List[str]],
  "alt_paths": List[List[str]],
  "ppr_scores": Optional[Dict],
  "current_notes": Optional[Dict],
  "campus_docs": Dict[str, List[Document]], // 校园文档库 (按类别)
  "campus_indexes": Dict[str, Index],       // 分类索引 {教务/生活/课程}
  "intent": Optional[str],                  // 意图分类结果
  "privacy_mode": bool,
  "interrupt_flag": bool,
  "extraction_confidence": float,
  "trace_id": str
}
```

---

## 5. 核心功能

### 5.1 规划师 Agent

- **双路交叉验证提取**：两个独立 LLM 并行提取课程信息 → Jaccard 比对 → 置信度分级决策
- **Node2Vec 链路预测**：发现缺失的先修关系，主动推荐给用户确认
- **知识图谱**：NetworkX 建模课程 / 教师 / 实验室 / 研究方向
- **课程详情**：信息卡 + 先修链 + 后继课程 + 相关教授 + 关联课程资料
- **路径推荐**：拓扑排序 → 学期约束 → 备用路径（用户标记冲突后自动切换）
- **课后复习**：笔记 → LLM + Embedding 匹配 → 图谱高亮标注

### 5.2 情报官 Agent

- **Personalized PageRank 推荐**：以用户当前课程/方向为起点，异构图上 PPR 传播，发现多跳关联
- **推送排序**：PPR 分数排行 + 推理链溯源展示
- **数据**：预置 JSON（主）+ 爬虫（辅）

### 5.3 学伴 Agent（Hybrid RAG + 共情）

- **Hybrid RAG 问答**：
  - Query Rewriting（多轮上下文压缩）
  - BM25 + Dense Embedding 双路检索
  - RRF 融合排序
  - LLM 生成带引用的回答
- **语义缓存**：相似问题秒回，零 LLM 调用
- **并行协作**：焦虑场景三路 Fan-out
- **共情生成**：LLM + 行为上下文

### 5.4 百事通 Agent（校园十万个为什么）⭐ 新增

这是本次升级的核心亮点，专门处理校园非结构化数据问答。

#### 5.4.1 三类数据全覆盖

| 分类 | 典型问题 | 数据来源 |
|------|---------|---------|
| **📋 教务通知** | "补考什么时候报名？""转专业需要什么条件？""四六级什么时候考？" | 教务处官网通知 PDF |
| **🏠 生活指南** | "快递站在哪？""哪个食堂窗口好吃？""校医院怎么报销？""校园卡丢了怎么办？" | 公众号/后勤官网 |
| **📚 课程资料** | "这门课期末考什么？""有往年试题吗？""课件在哪下载？" | 课程大纲/课件/复习资料 |

#### 5.4.2 核心交互流程

```
用户: "补考什么时候开始？"

[内部: 意图分类 → "教务类"]
[内部: 路由 → 教务索引 → BM25 + Dense 并行检索]
[内部: 时间衰减加权 → 最新补考通知得分最高]
[内部: 语义缓存 → 未命中]
[内部: RRF 融合 → Top-3]
[内部: LLM 生成]

系统: "
📋 教务通知 | 2026-02-20

根据教务处发布的《2025-2026学年第二学期补考安排通知》：
补考报名时间：2026年3月1日-3月5日
补考考试时间：2026年3月15日-3月20日

操作路径：登录教务系统 → 考试报名 → 补考确认 → 打印准考证

[来源: 教务处 → 考务专栏 → 2025-2026-2 补考通知 (第2节)]
[关联: 上学期该课程期末成绩 ≥ 45分方可报名补考]
"
```

#### 5.4.3 交互场景示例

**场景 1：新生问生活指南**
```
用户: "学校快递站在哪？"
系统: "
🏠 生活指南

五邑大学快递站位于北区学生宿舍 12 栋楼下（近北门）。
营业时间：9:00-19:00
快递类型：顺丰、京东、中通、圆通、韵达、极兔均支持。
取件方式：凭取件码到站自取，或加 2 元送上楼。

[来源: 后勤处 → 校园服务指南 2025-09 更新]
"
```

**场景 2：期末复习**
```
用户: "数据结构期末考什么题型？"
系统: "
📚 课程资料 | 数据结构（CS2101）

根据近三年考试资料分析：
1. 选择题（30分）— 基础概念、复杂度分析
2. 填空题（10分）— 关键定义
3. 简答题（20分）— 栈/队列/树/图的应用
4. 算法设计题（40分）— 必考：二叉树遍历 + 图最短路径

重点章节：第5章 二叉树（约占25%）、第7章 图（约占30%）

[来源: 数据结构 2023-2024 期末试卷 A卷]
[来源: 数据结构 2024-2025 期末试卷 B卷]
[关联: 课件第5章、第7章 → 点此查看]
"
```

**场景 3：模糊问题多源召回**
```
用户: "开学要做什么？"
系统: "
根据多个来源整理开学事项：

📋 教务通知 | 2026-02-15
【选课】第二轮选课时间：2月20日-2月28日

📋 教务通知 | 2026-02-10
【补考】补考报名：3月1日-3月5日（上学期挂科同学注意）

🏠 生活指南 | 2026-02-01
【食堂】第一食堂 2 月 18 日起恢复正常营业

[提示: 不同来源的结果已按相关性排序]
"
```

### 5.5 交互场景：Hybrid RAG 问答

```
用户: "这门课怎么样？"
系统: "你是指《数据结构》对吧？我来查一下。"

[内部: Query Rewriting → "数据结构这门课的教学内容、难度和评价如何？"]
[内部: BM25 Top-5 + Dense Top-5 → RRF → Top-3]
[内部: 语义缓存检查 → 未命中 → LLM 生成]

系统: "
根据课程大纲，数据结构包含以下内容：
1. 线性表、栈、队列、树、图等核心数据结构
2. 配套 3 个编程项目：表达式求值、哈夫曼编码、图遍历
3. 教材：严蔚敏《数据结构（C语言版）》
4. 先修课程：C语言程序设计
[来源: 数据结构课程大纲 第2-5节]
"
```

### 5.6 交互场景：Node2Vec 链路预测

```
系统: "我分析了一下已录入的课程关系，发现《机器学习》和《概率论》
       在内容上有较强的前后置关联（相关性 0.91），是否将《概率论》
       设为《机器学习》的先修课程？"
用户: "确认"
系统: "已加入图谱。当前《机器学习》的先修链更新为：
       高等数学 → 概率论 → 机器学习"
```

---

## 6. 冷启动引导（新用户首次体验）

| 步骤 | 操作 | 系统行为 |
|------|------|---------|
| 1 | 选择专业 | 展示本校专业列表 |
| 2 | 浏览课程体系 | 展示完整课程列表，按学期分组 |
| 3 | 标记已修 | 勾选已学完的课程 |
| 4 | 图谱渲染 | 渲染图谱 + Node2Vec 发现缺失先修 → 提示确认 |
| 5 | 进入主界面 | 课程地图 + 推荐路径 + RAG 问答 + **校园十万个为什么**入口 |

### 6.1 新增冷启动：校园十万个为什么首次引导

| 步骤 | 操作 | 系统行为 |
|------|------|---------|
| 1 | 首次进入校园十万个为什么 | 展示三类数据的概览卡片 + 热门问题推荐 |
| 2 | 尝试提问 | 系统演示一个典型问答（如"快递站在哪？"） |
| 3 | 浏览分类 | 用户可浏览教务通知/生活指南/课程资料列表 |
| 4 | 数据来源提示 | 显示数据覆盖范围、更新时间、置信度说明 |

---

## 7. 工业化设计

### 7.1 配置中心

统一管理所有配置，支持 YAML 文件 + 环境变量 + 运行时热加载：

```yaml
# config.yaml
app:
  name: GraphCampus
  version: 1.0.0
  debug: false

llm:
  provider: openai  # openai | qwen | deepseek
  model: gpt-4o-mini
  temperature: 0.3
  max_retries: 3
  backoff_base: 1.0
  circuit_breaker_threshold: 5
  circuit_breaker_reset_seconds: 30
  concurrency_limit: 3
  rate_limit_per_min: 10

embedding:
  model: sentence-transformers/all-MiniLM-L6-v2
  device: cpu

rag:
  bm25_top_k: 5
  dense_top_k: 5
  rrf_k: 60
  final_top_k: 3
  chunk_size: 512
  chunk_overlap: 50

campus_qa:
  enabled: true                          # 特征开关
  indexes:
    - academic                           # 教务索引
    - life                               # 生活索引
    - course                             # 课程索引
  time_decay_lambda: 0.01               # 时间衰减系数
  default_expiry_days: 365              # 默认有效期

cache:
  semantic_similarity_threshold: 0.92
  ttl_seconds: 3600
  max_size: 1000

database:
  path: data/graphcampus.db
  wal_mode: true

logging:
  level: INFO
  format: json
  output: console+file
```

- 启动时加载 YAML，环境变量覆盖（`.env`）
- 运行时通过 Feature Flag API 动态修改
- 前端管理页面可查看当前配置

### 7.2 特征开关（Feature Flag）

```python
# feature_flags.py
class FeatureFlags:
    flags = {
        "campus_qa": True,           # 校园十万个为什么
        "dual_validation": True,      # 双路交叉验证
        "node2vec": True,            # Node2Vec 链路预测
        "ppr_recommend": True,       # PPR 推荐
        "semantic_cache": True,      # 语义缓存
        "offline_mode": False,       # 离线模式（关闭所有 LLM）
        "strict_privacy": False,     # 严格隐私模式
    }

    @classmethod
    def is_enabled(cls, flag: str) -> bool:
        return cls.flags.get(flag, False)

    @classmethod
    def set_flag(cls, flag: str, value: bool):
        cls.flags[flag] = value
```

- 支持按功能、按用户、按环境灰度
- 前端可查看当前特征状态
- 答辩时可一键关闭/开启特定功能做对比演示

### 7.3 健康检查

```python
# health.py
@app.get("/health")
def health_check():
    return {
        "status": "ok",
        "version": "1.0.0",
        "checks": {
            "database": check_db(),
            "llm_service": check_llm(),
            "embedding_service": check_embedding(),
            "indexes": {
                "bm25": check_bm25_indexes(),
                "dense": check_dense_indexes(),
            },
            "cache": check_cache(),
        },
        "uptime_seconds": get_uptime(),
    }
```

- Streamlit 启动时显示健康状态
- 各组件不可用时前端显示降级提示
- Docker 容器存活检查

### 7.4 速率限制

```python
# rate_limiter.py
class RateLimiter:
    def __init__(self, max_requests: int = 10, window_seconds: int = 60):
        self.max_requests = max_requests
        self.window_seconds = window_seconds
        self.requests: Dict[str, List[float]] = defaultdict(list)  # user_id → timestamps

    def check(self, user_id: str) -> bool:
        now = time.time()
        window_start = now - self.window_seconds
        self.requests[user_id] = [t for t in self.requests[user_id] if t > window_start]
        if len(self.requests[user_id]) >= self.max_requests:
            return False  # 触发限流
        self.requests[user_id].append(now)
        return True
```

- 默认 10 次/分钟/用户
- 超过返回友好提示："操作太频繁，请稍后再试"
- 支持白名单（答辩演示时不限流）

### 7.5 错误码体系

| 错误码 | 含义 | 用户提示 |
|--------|------|---------|
| E001 | LLM 服务不可用 | AI 服务暂时不可用，请稍后再试 |
| E002 | Embedding 服务不可用 | 已切换为关键词模式 |
| E003 | 数据库连接失败 | 系统正在维护 |
| E004 | 检索无结果 | 未找到相关信息，换个问法试试 |
| E005 | 速率超限 | 操作太频繁，请稍后再试 |
| E006 | 文档解析失败 | 该文件格式不支持或已损坏 |
| E007 | 配置错误 | 系统配置异常，请联系管理员 |
| E008 | 特征未启用 | 该功能暂未开放 |

### 7.6 Pydantic 校验层

所有 I/O 数据用 Pydantic v2 定义，运行时强制校验：

```python
class CourseCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=100)
    code: str = Field(..., pattern=r"^[A-Z]+\d{4}$")
    credits: float = Field(..., ge=0.5, le=30)
    prerequisites: list[str] = Field(default_factory=list)
    semester: Literal["春季", "秋季", "春秋季"]
    teacher: str = Field(..., min_length=1)

class CampusDocument(BaseModel):
    doc_id: str = Field(..., pattern=r"^DOC_\d{8}$")
    category: Literal["academic", "life", "course"]
    title: str = Field(..., min_length=1, max_length=200)
    content: str = Field(..., min_length=1)
    source_url: Optional[str] = None
    publish_date: Optional[date] = None
    expiry_date: Optional[date] = None
    tags: list[str] = Field(default_factory=list)
    confidence: float = Field(default=1.0, ge=0, le=1)
```

- AgentState 进出 LangGraph 节点时自动校验
- 数据库写入前校验
- API 入参出参校验

### 7.7 LLM 调用层（重试 + 熔断 + 缓存 + 并发控制 + 速率限制）

```
LLM.call()
  ├── 速率限制检查 (10 req/min/user? → 继续; 否 → 429)
  ├── 特征开关检查 (对应功能已启用? → 继续; 否 → 降级)
  ├── 语义缓存查询 (相似度 > 0.92? → 直接返回)
  ├── 并发槽检查 (当前并行 < 3? → 继续; 否 → 排队)
  ├── 熔断器状态 (连续 5 次失败? → 30s 快速失败)
  ├── 重试 (3 次, 指数退避: 1s → 2s → 4s)
  ├── 计时 (写入结构化日志: prompt_tokens, latency)
  └── 语义缓存写入 (TTL=3600s)
```

### 7.8 结构化日志 + 性能追踪

每条请求生成 `trace_id`，贯穿所有节点。日志格式：

```json
{
  "ts": "2026-06-20T10:30:00.123Z",
  "level": "INFO",
  "trace_id": "abc123",
  "session_id": "sess_456",
  "user_id": "user_789",
  "node": "campus_qa_router",
  "latency_ms": 1560,
  "detail": {
    "intent": "academic",
    "index_used": "academic",
    "bm25_top_score": 0.85,
    "dense_top_score": 0.72,
    "rrf_top_score": 0.78,
    "cache_hit": false,
    "source_count": 3,
    "llm_tokens": 234
  }
}
```

- LangGraph 每个节点执行前后自动打点
- 可视化各节点耗时分布（答辩展示用）
- 支持按 trace_id 追踪单次请求全链路

### 7.9 测试体系

| 层级 | 工具 | 覆盖 | 负责人 |
|------|------|------|--------|
| 单元测试 | pytest | 每个 Pydantic model、每个 Node 函数、工具函数 | C |
| 集成测试 | pytest + SQLite in-memory | 子图全流程（Mock LLM）、十万个为什么管道测试 | C |
| 数据质量测试 | pytest | 分块质量检查、Embedding 质量、检索 MRR/Recall | C |
| 端到端测试 | pytest + Streamlit test | 用户操作路径 | C |
| CI | GitHub Actions | push/PR 触发，lint + test + build + 数据质量门禁 | A |

### 7.10 Docker + CI

```dockerfile
# 多阶段构建
FROM python:3.11-slim AS base
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

FROM base AS runtime
COPY --from=base /app /app
COPY . .
RUN python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('all-MiniLM-L6-v2')"
CMD ["streamlit", "run", "main.py", "--server.port=8501"]
```

```yaml
# .github/workflows/ci.yml
on: [push, pull_request]
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - run: pip install ruff pytest pytest-cov
      - run: ruff check .
      - run: pytest --cov=./ --cov-fail-under=80 --timeout=60
      - run: python -m scripts.check_data_quality  # 数据质量门禁
      - run: docker build -t graphcampus .

  deploy:
    needs: quality
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy placeholder"
```

---

## 8. 兜底设计（失败路径处理）

| 场景 | 兜底 |
|------|------|
| 双路 LLM 都提取错误 | 低置信度 → 强制手动编辑 |
| 单路提取错误 | 中置信度 → 展示争议项确认 |
| LLM 幻觉编造课程名 | 课程名校验（不在列表中→拒绝）+ 循环依赖检测 |
| Node2Vec 预测错误 | 预测结果仅"推荐"级别，需用户确认后才写入 |
| Hybrid RAG 检索不到 | BM25+Dense 双路同时空的概率极低；RRF 无结果 → "未找到" |
| RAG 检索分数低 | 低于阈值时提示换个问法 |
| **校园十万个为什么检索不到** | **提示"暂未收录该问题"，建议换个问法，并记录未命中日志供后续数据补充** |
| **文档解析失败** | **跳过该文档，记录错误日志，不影响其他文档索引** |
| **分类路由不确定** | **回退到全部索引检索 + 分数加权** |
| **过期通知** | **有效期过滤自动排除，并提示"该信息已过期"** |
| Embedding 服务不可用 | 回退到纯 BM25 关键词检索 |
| LLM API 超时/失败 | 重试 3 次 → 熔断 → 降级为规则回复 |
| LLM API 速率限制 | 排队等待或降级为缓存/规则回复 |
| 语义缓存污染 | TTL+LRU 淘汰，缓存表定期清理 |
| 配置错误 | 启动时校验，失败则使用默认配置 |
| 特征开关关闭某功能 | 前端对应入口隐藏，调用返回"该功能暂未开放" |

---

## 9. 隐私设计

| 数据 | 存储 | 上传 |
|------|------|------|
| 课程信息 | 本地 SQLite (WAL) | ❌ |
| 先修关系 | 本地 SQLite | ❌ |
| 已修标记 | 本地 SQLite | ❌ |
| 课堂笔记 | 本地处理 | ❌ |
| 行为日志 | 本地 SQLite | ❌ |
| LLM 调用 | 临时内存 | ✅ 仅结构化文本 |
| Embedding 调用 | 临时内存 | ✅ 仅文本片段，无身份 |
| 校园十万个为什么查询 | 临时内存 | ❌ 仅在本地索引检索 |

**隐私模式**：
- 离线模式：无 LLM，纯规则 + BM25 检索（校园十万个为什么仍可工作，使用 BM25 本地检索）
- 标准模式：默认，缓存命中优先
- 严格模式：每次 LLM 调用前确认

**数据安全**：
- 校园十万个为什么数据全部本地存储，不上传
- 非结构化数据索引构建全部在本地完成
- 用户查询记录不存储、不上传

---

## 10. 未来展望

| 方向 | 描述 |
|------|------|
| 学习小组匹配 | 基于课程图谱匹配同学 |
| 历史 QA 语料扩充 | 积累问答对，提升 RAG 覆盖率 |
| 教师评价分析 | 基于公开数据 |
| 知识图谱自动爬取 | 动态更新 |
| **校园十万个为什么自动爬取** | **定时爬取教务处/后勤通知，自动增量更新索引** |
| **多模态支持** | **支持图片（食堂菜单、校园地图）、语音提问** |
| **个性化推荐** | **根据用户提问历史，主动推送相关校园资讯** |

---

## 11. 5-6 周协同开发计划（三人版 · 30-36 天）

> A 每天 8-10h，B/C 每天各 3h。每周日同步 review。
> 总人力约 14-16h/天 × 30-36 天 ≈ 420-576 人时。

### 第 1 周：基建 + 接口锁死 + 数据管道

| 天 | 角色 A（8-10h） | 角色 B（3h） | 角色 C（3h） |
|----|----------------|-------------|-------------|
| **Day 1** | **三人共同锁死：AgentState + JSON Schema + Pydantic models + Mock 数据 + 表结构 + 配置中心定义 → README** | | |
| 2 | LLM 调用封装（重试/熔断/并发控制/速率限制） | Streamlit 骨架 + 路由 + 基础布局 | 非结构化数据管道原型（PDF 解析 + 文本清洗） |
| 3 | 结构化日志系统（tracer + trace_id）+ 配置中心实现 | 数据库建表 + CRUD + WAL 模式 | 智能分块策略实现（Layout-aware chunker） |
| 4 | 特征开关 + 健康检查 + 错误码体系 | 中断交互组件（弹窗 + 确认/编辑） | 元数据标注脚本 + 数据质量检查工具 |
| 5 | NetworkX 图谱构建 + Pyvis 序列化 | 课程详情页 + 图谱可视化 | 数据收集：下载教务处通知 50 篇 + 整理生活指南 |
| 6 | 拓扑排序 + Node2Vec 基础链路 | 冷启动引导（选专业 → 课程列表） | 数据收集：整理课程大纲 10 份 + 索引构建 |
| 7 | **周联调：录入→中断→图谱→前端** | 预置 15 门课 Mock 数据 | 数据质量报告 + 索引验证 |

### 第 2 周：双路验证 + Node2Vec + PPR + 校园数据就绪

| 天 | 角色 A（8-10h） | 角色 B（3h） | 角色 C（3h） |
|----|----------------|-------------|-------------|
| 8 | 双路 LLM 提取 + Jaccard 比对 + 置信度三级决策 | 双路结果 UI 展示（对比视图） | 教务通知索引构建 + 元数据清洗 |
| 9 | Node2Vec 链路预测 + 幻觉过滤 | 置信度标签 + 链路预测推荐弹窗 | 生活指南索引构建 + 分块质量评估 |
| 10 | Personalized PageRank 实现 + 推理链 | PPR 推荐结果前端（分数 + 排序） | 课程资料索引构建（首批 10 门课） |
| 11 | 拓扑 + 学期约束 + 备用路径 | 路径规划前端（方案 A/B 切换） | 数据采集：继续下载通知 50 篇 + 生活指南补充 |
| 12 | **百事通子图：意图分类 + 分类路由** | 校园十万个为什么基础 UI（分类 Tab） | 测试：数据管道单元测试 + 索引构建测试 |
| 13 | **百事通子图：时间感知排序 + 多源融合** | 校园十万个为什么问答输入框 + 展示区 | 测试：分块质量评估 + 检索 MRR/Recall |
| 14 | **本周联调 + Bug 清理 + 完整数据入库** | 校园十万个为什么 UI 完善 | 预置 40 门课完整数据 + 三类校园数据入库 |

### 第 3 周：Hybrid RAG + 语义缓存 + 百事通完整

| 天 | 角色 A（8-10h） | 角色 B（3h） | 角色 C（3h） |
|----|----------------|-------------|-------------|
| 15 | BM25 倒排索引构建（含分类索引） | RAG 问答界面（对话 UI）+ 十万个为什么 UI 完善 | 测试：BM25 索引正确性测试 |
| 16 | Dense Embedding 索引 + 余弦检索（含分类索引） | 双栏展示（BM25 / Dense 各自结果） | Embedding 质量验证（相似度分布检查） |
| 17 | RRF 融合排序 + LLM 生成（引用标注） | 引用标注前端（来源高亮）+ 分类标签 | 集成测试：RAG 全流程（Mock LLM） |
| 18 | Query Rewriting（多轮上下文压缩） | 多轮对话 UI（上下文展示） | 集成测试：十万个为什么全流程 |
| 19 | 语义缓存（相似度 > 0.92 + TTL + LRU） | 缓存命中提示 + 清零机制 | 测试：缓存命中率验证 + TTL 测试 |
| 20 | RAG + 缓存 + Query Rewriting 联调 | 十万个为什么来源引用卡片 UI | 端到端测试：用户操作路径 |
| 21 | **学伴子图完整化：RAG + 共情 + 行为统计** | 学伴对话界面 + 行为日志埋点 | **本周测试报告 + Bug 汇总** |

### 第 4 周：课后复习 + 并行 + 隐私 + 工业化

| 天 | 角色 A（8-10h） | 角色 B（3h） | 角色 C（3h） |
|----|----------------|-------------|-------------|
| 22 | 课后复习：笔记 LLM 提取 + Embedding 匹配 | 课后复习界面（粘贴 / 上传） | 测试：课后复习流程测试 |
| 23 | 知识点→图谱位置标注（前后继关联） | 图谱高亮 + 复习/预习建议卡 | 数据采集：补充课程资料 10 门课 |
| 24 | 滑动窗口 + 摘要（max_turns=20） | 摘要提示 UI | 测试：滑动窗口 + 摘要测试 |
| 25 | Fan-out / Fan-in 并行节点 | 并行协作前端（聚合展示） | 集成测试：Fan-out/Fan-in 全流程 |
| 26 | 隐私模式（离线 / 标准 / 严格） | 隐私控制面板 + 数据流向信息卡 | 测试：隐私模式降级验证 |
| 27 | **配置中心管理页面 + 特征开关 API** | 配置/Fature Flag 管理前端 | 文档：API 文档 + 配置说明 |
| 28 | **完整联调：四 Agent 全流程贯通** | UI 统一打磨 + 十万个为什么体验优化 | **全量回归测试 + 覆盖率统计** |

### 第 5 周：工业化 + 边界测试 + 数据完善

| 天 | 角色 A（8-10h） | 角色 B（3h） | 角色 C（3h） |
|----|----------------|-------------|-------------|
| 29 | 测试框架：pytest + 单元测试（Model / Node） | Docker 镜像构建 + 本地测试 | 补充测试：边界情况 + 异常场景 |
| 30 | LLM 调用层边界（超时 / 空响应 / 异常） | 兜底交互全面对齐 + 错误提示美化 | 测试：速率限制 + 熔断测试 |
| 31 | Hybrid RAG 质量调优（chunk / Top-K / RRF k） | 图谱交互增强（搜索 / 高亮） | 数据质量报告 + 索引优化建议 |
| 32 | PPR / Node2Vec 性能测试（40 节点） | UI 统一（配色 / 间距 / 字体 / 移动适配） | **测试覆盖率冲刺 > 80%** |
| 33 | **校园十万个为什么质量调优（分块策略 / 路由准确率 / 时间衰减系数）** | 十万个为什么交互细节打磨（空状态 / 加载态 / 错误态） | 数据补充：收集更多校园数据 |
| 34 | Docker 多阶段构建 + GitHub Actions CI | Docker + CI 联调 | 最终数据质量验证 |
| 35 | **最终 Bug 清理 + 全场景回归** | 演示数据准备 + 操作剧本 | **最终测试报告** |

### 第 6 周（可选/缓冲）：文档 + 答辩

| 天 | 角色 A（8-10h） | 角色 B（3h） | 角色 C（3h） |
|----|----------------|-------------|-------------|
| 36 | 互相黑盒测试 + 最终修 Bug | 演示视频录制（5 分钟） | 互相黑盒测试 |
| 37 | 技术白皮书（架构图 / 算法 / 工业化设计） | PPT 制作 + 演示彩排 | 技术白皮书协作 + README 最终完善 |
| 38 | 模拟答辩（A 讲算法/工业化） | 模拟答辩（B 操作演示） | 模拟答辩（C 辅助 Q&A） |
| 39 | 第二轮模拟答辩 + 提交准备 | 视频最终剪辑 | 所有文档归档 |
| 40 | **提交** | **提交** | **提交** |

---

## 12. 交付物清单

### 代码

| 模块 | 说明 | 负责人 |
|------|------|--------|
| `main.py` | 入口 | A |
| `agents/` | LangGraph 子图 + 父图（含新增百事通子图） | A |
| `curriculum/` | 双路验证 + 置信度 + Node2Vec | A |
| `knowledge_graph/` | NetworkX + PPR + NVec | A |
| `rag/` | BM25 + Dense + RRF + Rewrite + 语义缓存 | A |
| `campus_qa/` | **校园十万个为什么：意图分类 + 分类路由 + 多源融合 + 时间排序** | A |
| `review/` | 课后复习 | A |
| `crawler/` | 爬虫 + JSON 数据 | B |
| `data_pipeline/` | **非结构化数据处理：文档解析 + 分块 + 元数据标注 + 索引构建** | C |
| `database/` | 建表 + CRUD + WAL | B |
| `utils/` | LLM 中枢、配置中心、特征开关、健康检查、速率限制、tracer、日志 | A |
| `models/` | Pydantic v2 定义 | A |
| `tests/` | pytest 测试（单元/集成/E2E/数据质量） | C |
| `config/` | **配置中心 YAML + Feature Flag 定义** | A |
| `scripts/` | **数据质量检查、索引构建、数据导入脚本** | C |
| `docker/` | Dockerfile + .dockerignore | B |
| `.github/` | GitHub Actions CI | B |

### 数据

| 交付物 | 说明 | 负责人 |
|--------|------|--------|
| 专业课数据 | 一个完整专业 ~40 门课（含先修关系标注） | C |
| **教务通知数据** | **历年通知 200+ 篇（清洗 + 分块 + 元数据标注）** | C |
| **生活指南数据** | **校园生活指南 100+ 篇** | C |
| **课程资料数据** | **首批 10 门课的课程资料（含期末试题）** | C |

### 文档

| 交付物 | 说明 | 负责人 |
|--------|------|--------|
| `README.md` | 项目简介 + Day 1 接口定义 + 环境配置 | A |
| `项目规划书.md` | 本文档 | A |
| `技术白皮书.pdf` | 架构 / 算法 / 工业化设计 / 性能数据 | A + C |
| `演示视频.mp4` | 5 分钟（含校园十万个为什么演示） | B |
| `答辩PPT.pptx` | 含新增功能亮点 | B |

---

## 13. 协作规范

- **Git**：GitHub 私有库，`feat/xxx` 分支，PR review 后合并
- **Day 1 锁死**：AgentState + Schema + Pydantic models + Mock 数据 + 配置定义 → README
- **日志**：统一 `trace_id`，JSON 格式
- **沟通**：每日 10 分钟站会（A 主持），周日 30 分钟同步 review
- **任务追踪**：使用 GitHub Projects / Todo List 跟进每日进度
- **代码审查**：每个 PR 至少 1 人 review，核心模块需 A review
- **数据质量门禁**：CI 中自动检查数据质量（分块完整性、索引覆盖率）

---

## 14. 答辩策略

### 角色

| 角色 | 主讲 |
|------|------|
| **A** | 双路验证 / Node2Vec / PPR / Hybrid RAG / **校园十万个为什么核心算法** / 工业化设计 |
| **B** | 痛点 / 交互 / 演示操作 / 校园十万个为什么前端展示 |
| **C** | 数据采集与处理 / 测试体系 / 数据质量 / Q&A |

### 演示流程（7 分钟，含新增亮点）

| 时间 | 环节 | 主讲 |
|------|------|------|
| 0:00-0:30 | 痛点 | B |
| 0:30-1:00 | 冷启动（选专业 + 标记已修 + 图谱渲染） | B 操作，A 解说 |
| 1:00-2:00 | **亮点 1：双路验证 + Node2Vec 链路预测** — 两路提取对比 + 置信度标签 + 系统推荐缺失先修 | A |
| 2:00-2:45 | **亮点 2：PPR 推荐** — 推理链溯源 | A |
| 2:45-3:45 | **⭐ 亮点 3：校园十万个为什么** — 三类数据现场演示（问教务通知 + 问生活指南 + 问课程资料），展示分类标签、来源引用、时间标签 | B 操作，A 解说 |
| 3:45-4:30 | **亮点 4：Hybrid RAG 问答** — 现场提问 + BM25/Dense 双栏 + 引用标注 + 语义缓存秒回 | B |
| 4:30-5:00 | 课后复习 | B |
| 5:00-5:45 | 工业化 + 兜底 + 隐私 — 结构化日志 / 配置中心 / 特征开关 / 测试覆盖率 / 熔断降级 | A |
| 5:45-6:00 | 数据质量 + 测试体系 | C |
| 6:00-6:30 | 总结 | A+B |
| 6:30-7:00 | Q&A | 三人共同 |

### Q&A

| 问题 | 要点 | 主讲 |
|------|------|------|
| 先修关系怎么保证准确？ | 双路 LLM 交叉验证 + 幻觉过滤 + Node2Vec 辅助发现 | A |
| 推荐为什么用 PPR？ | 多跳隐性关联 + 天然排序，比规则灵活 | A |
| RAG 比直接问 LLM 好在哪？ | BM25+Dense 双路检索 + RRF + 引用可溯源 + 语义缓存零延迟 | A |
| **校园十万个为什么的数据怎么来的？** | **爬虫 + 手动整理 + LLM 清洗，三类数据分别处理，200+ 通知、100+ 生活指南、10+ 课程资料** | C |
| **如何保证回答的时效性？** | **时间感知排序 + 有效期过滤 + 过期提示** | A |
| **跟直接搜教务处网站有什么区别？** | **自然语言直接问、多源聚合、带引用溯源、语义缓存秒回** | B |
| 你们和百度文库的区别？ | 图谱关系 + PPR + RAG，不是一个维度 | A |
| Node2Vec 是什么？ | 图嵌入 + 链路预测，主动发现缺失的先修关系 | A |
| 工业化体现在哪？ | Pydantic 校验、配置中心、特征开关、重试熔断缓存、结构化日志、>80% 测试覆盖、Docker + CI | A |
| 三人如何协作？ | A 主力 8-10h/天核心算法，B 前端 3h/天，C 数据+测试 3h/天，每日站会 | C |

---

## 附录 A：快速环境配置

```bash
git clone https://github.com/your-org/graphcampus.git
cd graphcampus
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
# 填入 LLM_API_KEY
streamlit run main.py
```

## 附录 B：依赖清单

```
streamlit>=1.28
langgraph>=0.0.20
langchain>=0.1.0
openai>=1.6
networkx>=3.1
pyvis>=0.3.1
sentence-transformers>=2.2
rank-bm25>=0.2.2
node2vec>=0.4.0
pydantic>=2.0
structlog>=24.0
pytest>=8.0
pytest-cov>=5.0
pillow>=10.0
beautifulsoup4>=4.12
pypdfium2>=5.0
python-docx>=1.1
pyyaml>=6.0
```

## 附录 C：本校数据源（待填）

| 数据源 | URL | 状态 | 负责人 |
|--------|-----|------|--------|
| 教务处培养方案 | 待填 | ⏳ | C |
| 课程大纲 | 待填 | ⏳ | C |
| 教师主页 | 待填 | ⏳ | C |
| 讲座公告 | 待填 | ⏳ | B |
| **教务处通知列表** | **待填** | **⏳** | **B** |
| **后勤/生活指南** | **待填** | **⏳** | **B** |
| **课程资料收集** | **待填** | **⏳** | **C** |

## 附录 D：Day 1 锁死清单

| 产出 | 说明 | 状态 |
|------|------|------|
| AgentState | 含 trace_id / 缓存 / 图模型引用 / 分类索引 | ⏳ |
| JSON Schema | 四 Agent 入参出参（含百事通） | ⏳ |
| Pydantic models | Course / Teacher / Lab / Event / Log / CampusDocument / Chunk | ⏳ |
| 配置中心定义 | config.yaml 骨架 + feature_flags.py | ⏳ |
| Mock 数据 | 15 门课 + Embedding 样例 + 校园文档 30 篇 | ⏳ |
| 表结构 | courses / teachers / labs / events / logs / cache / embeddings / documents / chunks | ⏳ |
| README | 以上全部写入 | ⏳ |

---

> 本文档由团队共同维护，随项目推进持续更新。
> 最后更新：2026 年 6 月
