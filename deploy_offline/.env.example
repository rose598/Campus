# LLM 密钥配置模板 —— 复制为项目根目录的 .env 并填入真实 Key
# .env 已被 .gitignore 排除，不会提交到仓库
LLM_API_KEY=在此填入你的sk-xxx
# 可选覆盖项（默认值已在 config/config.yaml）
# LLM_BASE_URL=https://api.llm.ustc.edu.cn/v1
# LLM_MODEL=deepseek-chat
