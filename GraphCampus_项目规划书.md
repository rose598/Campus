# GraphCampus 项目规划书

> 基于状态图的多智能体校园助手 —— 双人协同开发版（[学校名]定制）

---

## 目录

1. [项目概述](#1-项目概述)
2. [数据来源](#2-数据来源)
3. [团队分工](#3-团队分工)
4. [技术架构](#4-技术架构)
5. [核心功能](#5-核心功能)
6. [兜底设计（失败路径处理）](#6-兜底设计失败路径处理)
7. [隐私设计](#7-隐私设计)
8. [未来展望（不作本次开发承诺）](#8-未来展望不作本次开发承诺)
9. [6 周协同开发计划](#9-周协同开发计划双人版)
10. [交付物清单](#10-交付物清单)
11. [协作规范](#11-协作规范)
12. [答辩策略](#12-答辩策略)

---

## 1. 项目概述

### 1.1 一句话概括

**GraphCampus** 是一个基于状态图的多智能体校园助手，由"规划师""情报官""学伴"三个 AI 智能体组成。它能自动解析各专业课的大纲内容、梳理课程间的先修与知识依赖关系、根据你的专业方向推荐最优学习路径，并主动推送与你所学课程相关的讲座、竞赛和科研机会。

### 1.2 解决的问题

| 场景 | 痛点 |
|------|------|
| 选课迷茫 | 只看到一门课的名字，不知道具体讲什么、难不难、值不值得选 |
| 先修关系不清 | 选了某门课才发现需要另一门课的基础，跟不上 |
| 学完不知道下一步 | 学完一门专业课，不知道接下来该学什么能形成知识体系 |
| 课程与资源脱节 | 不知道这门课对应的教授在做什么研究、实验室是否招新 |
| 信息茧房 | 有相关讲座/竞赛但完全不知道，错过了才后悔 |
| 期末焦虑 | 专业课压力大，深夜没人能说，也不知道该怎么办 |

### 1.3 核心价值

帮你把零散的专业课信息连成一张网——课程讲什么、和什么课有关、谁在教、学了能去哪——一目了然。

### 1.4 本校化定位

- 仅服务 **本校**，所有专业课数据来源于本校培养方案和课程大纲
- 知识图谱预置本校教师、课程、实验室、研究方向的真实关系
- 开发期间即可收集真实课程数据，演示时展示本校课程体系

---

## 2. 数据来源

| 数据 | 来源 | 获取方式 | 可行性 |
|------|------|---------|--------|
| 专业课列表 | 教务处官网 / 培养方案 PDF | 手动下载 → 解析为结构化数据 | 每专业一份，一次解析即可 |
| 课程大纲 | 教务处 / 学院网站 / 教师提供 | 下载 PDF → LLM + 正则提取 | 格式相对统一，可批量处理 |
| 先修关系 | 培养方案中的课程关系图 / 课程大纲 | 手动标注 + LLM 辅助提取 | 每专业几十门课，标注成本低 |
| 授课教师 | 教务处排课数据 | 手动录入 / 课程大纲中提取 | 配套课程一起录入 |
| 实验室 / 研究方向 | 学院官网、教师主页 | BeautifulSoup 爬取 | 本院官网结构固定 |
| 讲座 / 竞赛通知 | 学校官网、团委公众号 | BeautifulSoup 爬取指定页面 | 本校官网可定向适配 |
| 已修课程 | 学生手动标记 | 界面勾选已修课程 | 用户主动操作，不涉及教务系统 |
| 行为日志 | 与本系统的交互 | 本地埋点记录 | 完全本地，不上传 |

**关键约定**：所有数据要么来自公开信息（官网），要么由学生主动提供。不涉及任何未授权的数据采集。

---

## 3. 团队分工

### 角色 A：Agent 与算法负责人（偏后端/算法）

| 职责 | 具体内容 |
|------|---------|
| LangGraph 工作流编排 | 定义父图与子图结构、节点路由、状态迁移 |
| 多 Agent 协作逻辑 | 实现 Agent 间消息传递、中断与恢复机制 |
| 专业课解析引擎 | PDF/文本 → 课程结构化信息提取（LLM + 规则） |
| 课程知识图谱构建 | NetworkX 实体建模（课程/先修/教师/实验室/方向）+ 路径推理 |
| 学习路径推荐 | 基于先修关系 DAG 的拓扑排序 + 用户进度匹配 |
| 行为统计 | Pandas 计算使用频率、访问模式等 |
| LLM 提示词工程 | 课程解析、推理、生成三类提示词的设计与调优 |

### 角色 B：全栈体验与场景负责人（偏前端/产品）

| 职责 | 具体内容 |
|------|---------|
| Streamlit 前端交互 | 页面路由、组件设计、状态同步 |
| 课程知识图谱可视化 | NetworkX → 前端图展示（课程节点 + 连线关系） |
| 校园信息抓取 | BeautifulSoup 爬取本校官网/学院页/公众号 |
| 数据库设计与 API 封装 | SQLite 表结构、CRUD 操作封装 |
| 用户行为日志埋点 | 点击、查看、标记操作的本地记录 |
| 课程详情页设计 | 课程卡片、大纲展示、先修/后继课程导航 |
| 演示剧本撰写与 UI/UX | 交互流程设计、视觉风格统一、演示准备 |

### 公共模块

- AgentState 字段定义
- 子图输入/输出 JSON Schema
- LLM 中枢封装接口
- 数据库 Schema
- 隐私设计原则

---

## 4. 技术架构

### 4.1 整体架构图

```
┌──────────────────────────────────────────────────────────┐
│                     GraphCampus                           │
│                                                          │
│   ┌──────────────────────────────────────────┐           │
│   │              Streamlit 前端                │           │
│   │  ┌──────┐ ┌──────┐ ┌──────┐ ┌─────────┐ │           │
│   │  │课程地图│ │课程详情│ │推送面板 │ │对话面板  │ │           │
│   │  └──┬───┘ └──┬───┘ └──┬───┘ └────┬────┘ │           │
│   └─────┼────────┼────────┼───────────┼──────┘           │
│         │        │        │           │                  │
│   ┌─────┴────────┴────────┴───────────┴──────┐           │
│   │           LangGraph 工作流引擎             │           │
│   │                                          │           │
│   │  ┌──────────┐  ┌──────────┐  ┌────────┐ │           │
│   │  │ 规划师子图 │  │ 情报官子图 │  │ 学伴子图│ │           │
│   │  │ 课程解析   │  │ 信息推送   │  │情绪支持 │ │           │
│   │  │ 路径推荐   │  │ 机会发现   │  │ 对话   │ │           │
│   │  └─────┬────┘  └────┬─────┘  └───┬────┘ │           │
│   │        └────────────┼─────────────┘      │           │
│   │                     │                     │           │
│   │           ┌─────────┴─────────┐          │           │
│   │           │    AgentState      │          │           │
│   │           │   (共享状态层)      │          │           │
│   │           └─────────┬─────────┘          │           │
│   └─────────────────────┼────────────────────┘           │
│                         │                                │
│   ┌─────────────────────┼────────────────────┐           │
│   │                     │                     │           │
│   │  ┌──────────────────┴──────────────────┐ │           │
│   │  │           LLM 服务层                  │ │           │
│   │  │  (OpenAI API / 脱敏后调用)            │ │           │
│   │  └─────────────────────────────────────┘ │           │
│   │                                          │           │
│   │  ┌───────────┐ ┌───────────┐ ┌────────┐ │           │
│   │  │  SQLite    │ │ NetworkX  │ │  日志   │ │           │
│   │  │  数据库    │ │  课程图谱  │ │  文件   │ │           │
│   │  └───────────┘ └───────────┘ └────────┘ │           │
│   └──────────────────────────────────────────┘           │
└──────────────────────────────────────────────────────────┘
```

### 4.2 技术栈选型

| 层次 | 技术选型 | 说明 |
|------|---------|------|
| 前端框架 | **Streamlit** | 快速迭代，Python 原生 |
| 工作流引擎 | **LangGraph** | 有向状态图，支持中断/恢复/人工介入 |
| 大模型 | **OpenAI API / 国产大模型** | 课程语义提取、解析、生成；仅传脱敏文本 |
| 知识图谱 | **NetworkX + Pyvis** | 课程关系建模 + 前端可视化 |
| 数据库 | **SQLite** | 本地优先，无需部署 |
| 爬虫 | **BeautifulSoup** | 爬取学院官网、教师主页、讲座通知 |
| PDF 解析 | **pdfplumber** | 课程大纲 PDF 结构化提取 |
| 语音 | **Whisper API + gTTS** | 语音交互入口 |
| 路径推荐 | **纯 Python（拓扑排序 + 约束匹配）** | 基于先修 DAG 的最优路径计算 |

### 4.3 LangGraph 状态图设计

```
ParentGraph (协作父图)
│
├── Input: 课程大纲PDF / 文本 / 语音 / 已修课程标记
│
├── Node 1: 解析节点 (Parse)
│   ├── SubNode: 课程大纲PDF解析 (提取课程名/学分/先修/内容大纲/教师)
│   ├── SubNode: LLM 结构化补全 (PDF中缺失字段的智能补充)
│   └── SubNode: 先修关系推理 (LLM: 识别大纲中的"先修课程"字段)
│
├── Node 2: 调度与分发 (Router)
│   ├── → 规划师子图 (课程入库 / 路径更新 / 推荐请求)
│   ├── → 情报官子图 (匹配相关机会 / 推送)
│   └── → 学伴子图 (学习反馈 / 情绪支持)
│
├── Node 3: 各子图执行
│   │
│   ├── Agent_1: 规划师子图
│   │   ├── 课程图谱更新 (新课程节点 + 边关系写入)
│   │   ├── 学习路径计算 (拓扑排序 → 最优学习顺序)
│   │   └── 选课建议生成 (已修+待修 → 推荐下学期课程)
│   │
│   ├── Agent_2: 情报官子图
│   │   ├── 课程→教师→实验室→研究方向 路径匹配
│   │   ├── 讲座/竞赛匹配 (关键词 + 专业方向)
│   │   └── 推送理由生成 (推理链)
│   │
│   └── Agent_3: 学伴子图
│       ├── 学习行为统计
│       └── LLM 共情生成 → 学习建议输出
│
├── Node 4: 结果聚合 (Aggregation)
│   └── 合并各Agent输出 → 最终响应
│
└── Output: 课程图谱 / 课程详情 / 推送列表 / 对话

状态定义 (AgentState):
{
  "user_id": str,
  "messages": List[BaseMessage],
  "courses": Dict[str, Course],         // 所有已录入专业课 {course_id: Course}
  "course_graph": Graph,                // NetworkX 课程关系图
  "user_progress": Dict,                // 已修/在修/待修课程列表
  "learning_path": Optional[List[str]], // 推荐学习路径 (课程ID列表)
  "knowledge_graph": Graph,             // 完整知识图谱 (含教师/实验室/方向)
  "behavior_logs": List[Log],           // 行为日志
  "pending_questions": List[str],       // 待询问用户的问题
  "privacy_mode": bool,                 // 隐私模式开关
  "interrupt_flag": bool                // 是否触发中断
}

// Course 结构
{
  "id": str,
  "name": str,
  "code": str,                          // 课程代码
  "credits": float,
  "prerequisites": List[str],           // 先修课程ID列表
  "follow_ups": List[str],              // 后继课程ID列表 (从其他课程的prerequisites推导)
  "outline": List[str],                 // 内容大纲要点
  "teacher": str,
  "department": str,
  "semester": str,                      // 通常开课学期
  "exam_form": str,                     // 考核方式
  "textbook": str,                      // 推荐教材
  "related_labs": List[str],            // 相关实验室
  "related_directions": List[str]       // 相关研究方向
}
```

---

## 5. 核心功能

### 5.1 规划师 Agent（课程解析 + 路径推荐）

- **课程大纲解析**：导入课程大纲 PDF/文本，自动提取课程名、学分、先修课程、内容大纲、考核方式、授课教师、推荐教材
- **课程知识图谱构建**：将各专业课建模为图节点，先修关系为有向边，自动构建课程关系网络
- **课程详情呈现**：每门课的完整信息卡，含先修课程链、后继课程、相关教授与实验室
- **学习路径推荐**：基于先修关系 DAG 执行拓扑排序，结合用户已修课程，推荐当前学期和下学期的最优选课顺序
- **课程关联发现**：展示"学了这门课，你还能学什么"的扩展路径

### 5.2 情报官 Agent（机会发现与推送）

- **知识图谱匹配**：基于课程→教师→实验室→研究方向的图路径，精准匹配相关机会
- **讲座推送**：检测到与某门课内容相关的讲座时主动推送，附带推理链
- **竞赛推荐**：匹配课程方向相关的学科竞赛
- **实验室招新推送**：当某门课的授课教师所在实验室招新时通知学生

### 5.3 学伴 Agent（学习陪伴）

- **学习行为统计**：记录课程查看频率、学习路径调整次数
- **共情回复生成**：LLM 基于用户输入 + 学习上下文生成个性化回应
- **学习建议**：在用户感到迷茫时，基于当前进度给出具体的下一步建议

### 5.4 多 Agent 协作流程示例

用户说"这学期课好难，不知道该怎么学"：

1. **学伴** 感知到困惑 → 向 **规划师** 请求当前学期课程和先修关系
2. **规划师** 返回课程知识图谱子图（当前课程 + 前置基础课）
3. **学伴** 将课程难度 + 先修缺失情况拼接成上下文 → 调用 LLM 生成学习建议
4. **情报官** 被触发 → 搜索相关辅导讲座、学习小组 → 推送给用户

### 5.5 核心交互场景：课程探索

1. 用户导入一份专业课大纲 PDF（或从预置列表中选择课程）
2. 系统自动解析，生成完整课程信息卡
3. 用户切换到"课程地图"视图，看到该课程在专业课程体系中的位置
4. 点击前置课程 → 跳转详情；点击后继课程 → 了解后续方向
5. 系统推荐"和这门课最相关的 3 门课"
6. 情报官显示"这门课相关的讲座/竞赛/实验室机会"

---

## 6. 兜底设计（失败路径处理）

| 场景 | 失败可能 | 兜底方案 |
|------|---------|---------|
| 课程 PDF 解析 | PDF 是扫描版 / 格式混乱 | 回退到手动填写表单（课程名、学分、先修等字段） |
| 先修关系提取 | LLM 提取错误或遗漏 | 展示并允许用户编辑先修课程列表；手动添加/删除边 |
| 课程图谱构建 | 循环依赖出现 | 检测到环后标记并提示用户确认 |
| 爬取教师主页 | 页面结构变化 / 无法访问 | 静态 fallback + 支持手动录入 |
| 爬取讲座通知 | 网站改版 / 网络不通 | 显示"暂无最新通知"，不影响其他功能 |
| LLM API 调用失败 | 超时 / KEY 失效 | 缓存本地基础回复 + 提示服务暂不可用 |

**原则**：每个数据入口都提供手动编辑/输入的备选路径。不假设系统 100% 正确。

---

## 7. 隐私设计

| 数据类型 | 存储位置 | 是否上传 | 说明 |
|---------|---------|---------|------|
| 课程大纲 PDF | 本地处理后存结构化数据 | ❌ 原始 PDF 不上传 | 解析完成后仅存结构化数据 |
| 已修课程标记 | 本地 SQLite | ❌ 不上传 | 用户手动勾选 |
| 学习路径 | 本地 SQLite | ❌ 不上传 | 系统计算结果 |
| 行为日志 | 本地 SQLite | ❌ 不上传 | 点击/查看记录 |
| LLM 调用内容 | 临时内存 | ✅ 仅传脱敏文本 | 如 `{"course":"数据结构","action":"parse_outline"}` |
| 爬取的公开信息 | 本地 SQLite | ❌ 不上传 | 校内公开数据 |

### 隐私模式选项

| 模式 | LLM 调用 | 功能降级 |
|------|---------|---------|
| **完全离线** | 不调用，纯规则解析 | 课程解析靠正则，推送靠关键词匹配 |
| **标准模式**（默认） | 调用 API，传脱敏文本 | 完整功能 |
| **严格模式** | 每次调用前需确认 | 交互稍繁琐 |

---

## 8. 未来展望（不作本次开发承诺）

| 方向 | 描述 |
|------|------|
| 成绩预测 | 基于课程历史给分数据（公开信息），预测课程难度 |
| 学习小组匹配 | 找到正在学同一门课的同学 |
| 课程问答社区 | 每门课下的问答讨论区 |
| 校园服务集成 | 校车、食堂、图书馆等轻量查询 |

---

## 9. 6 周协同开发计划（双人版）

> 每天投入 2-3 小时，每周日同步进度。

### 第 1 周：基础建设与课程数据入库

| 日期 | 角色 A（Agent 与算法） | 角色 B（全栈体验与场景） |
|------|------------------------|--------------------------|
| Day 1 | 搭 LangGraph 最小图，定义 AgentState + Course 结构 | 搭 Streamlit 项目骨架，配置路由 |
| Day 2 | 课程 PDF 解析节点（pdfplumber + LLM 提取关键字段） | 数据库建表：courses / prerequisites / teachers / labs / events / logs |
| Day 3 | 先修关系提取 + 结构化补全（LLM + 规则） | 课程详情页设计（信息卡 + 先修导航） |
| Day 4 | NetworkX 课程图谱构建（节点 + 有向边） | 图谱可视化（Pyvis 前端渲染课程关系图） |
| Day 5 | 拓扑排序实现（先修 DAG → 学习路径计算） | 录入本校 1-2 个专业课程数据（约 30 门课） |
| Day 6 | 课程解析 + 图谱 + 路径推荐联调 | 课程地图页面集成：图谱 + 详情 + 路径展示 |
| Day 7 | **周联调**：解析→入库→图谱→前端展示全流程 | 兜底交互：手动录入表单、编辑先修关系 |

**本周交付**：可展示本校某专业的课程地图，支持课程搜索、详情查看、先修关系浏览。

---

### 第 2 周：三 Agent 核心 + 情报官数据

| 日期 | 角色 A（Agent 与算法） | 角色 B（全栈体验与场景） |
|------|------------------------|--------------------------|
| Day 8 | 规划师子图完整化（课程解析 → 图谱更新 → 路径推荐 全流程） | 前端"我的学习路径"展示（当前进度 + 推荐选课列表） |
| Day 9 | 构建完整知识图谱（加入教师/实验室/研究方向实体和关系） | 情报官前端面板：推送列表 + 推理链展开 |
| Day 10 | 图谱路径推理匹配节点（课程→教师→实验室→机会） | 爬取学院官网教师主页 + 研究方向 |
| Day 11 | 情报官子图完整化（匹配 + 排序 + 推送生成） | 爬取本校讲座/竞赛通知，填充数据 |
| Day 12 | 学伴行为统计节点（查看频率、路径修改记录） | 行为日志埋点：记录课程查看、路径调整操作 |
| Day 13 | 学伴子图完整化（LLM 共情生成 + 学习建议） | 学伴对话界面 + 学习建议展示 |
| Day 14 | **三 Agent + 课程地图全流程联调** | 前端集成所有面板，统一 UI |

**本周交付**：三 Agent 可协作运行，课程地图可交互浏览，情报官可推送本校机会。

---

### 第 3 周：打磨、语音、演示准备

| 日期 | 角色 A（Agent 与算法） | 角色 B（全栈体验与场景） |
|------|------------------------|--------------------------|
| Day 15 | 课程解析边缘情况处理（非常规 PDF / 缺失字段） | 语音输入集成 + 兜底文字回退 |
| Day 16 | 学习路径推荐逻辑优化（学分约束 + 开课学期匹配） | TTS 播报今日学习建议 |
| Day 17 | 图谱路径推理优化（多跳匹配排序） | 前端图谱交互增强（缩放/搜索/高亮路径） |
| Day 18 | 隐私模式切换逻辑实现 | 隐私模式前端控制 + 数据流向提示 |
| Day 19 | 全流程异常处理 + LLM 降级 | 兜底交互完善（每个入口对齐回退方案） |
| Day 20 | 系统集成测试 + 边界情况演练 | UI/UX 统一优化，移动端基本适配 |
| Day 21 | **模拟演示跑三遍，修 Bug** | 撰写演示剧本，准备演示数据 |

**本周交付**：完整可演示系统。

---

### 第 4 周：测试、文档与答辩

| 日期 | 协同任务 |
|------|----------|
| Day 22 | 互相黑盒测试，记录 Bug |
| Day 23 | 修 Bug，边界情况处理 |
| Day 24 | 撰写《技术白皮书》 |
| Day 25 | 录制 5 分钟演示视频 |
| Day 26 | 制作答辩 PPT |
| Day 27 | 模拟答辩，互相提问 |
| Day 28 | 最终检查、提交 |

---

## 10. 交付物清单

### 代码与系统

| 交付物 | 说明 |
|--------|------|
| `graphcampus/` 项目仓库 | 完整 Python 源码 |
| `main.py` | Streamlit 应用入口 |
| `agents/` | 三个子图 + 父图 LangGraph 定义 |
| `curriculum/` | 课程 PDF 解析、结构化提取模块 |
| `knowledge_graph/` | NetworkX 课程图谱 + 知识图谱构建与查询 |
| `scheduler/` | 学习路径推荐（拓扑排序 + 约束匹配） |
| `crawler/` | 本校官网/教师主页/讲座爬虫 |
| `database/` | SQLite 建表与 CRUD |
| `utils/` | LLM 封装、日志、工具函数 |

### 文档

| 交付物 | 说明 |
|--------|------|
| `README.md` | 项目简介、快速启动、架构概览 |
| `项目规划书.md` | 本文档 |
| `技术白皮书.pdf` | 架构图、数据流、隐私设计 |
| `演示视频.mp4` | 5 分钟演示 |
| `答辩PPT.pptx` | 答辩演示文稿 |

---

## 11. 协作规范

### 11.1 代码协作

- **版本控制**：GitHub 私有仓库，各自分支，每周合并到 main
- **分支命名**：`feat/xxx`、`fix/xxx`、`docs/xxx`
- **Commit 规范**：`[模块] 描述`
- **代码审查**：合并前交叉 review

### 11.2 接口约定

- AgentState 定义写入 README
- 子图输入/输出统一 JSON Schema
- LLM 调用：`llm_call(system_prompt, user_message) -> str`
- 数据库：`db.get_course(course_id)`、`db.get_user_path(user_id)`

### 11.3 沟通机制

- **每日站会**：晚上 10 分钟
- **周日同步**：30 分钟周会
- **即时沟通**：微信/飞书

---

## 12. 答辩策略

### 12.1 角色分配

| 角色 | 主讲内容 |
|------|---------|
| **角色 A** | 系统架构、课程图谱算法、学习路径推荐、LLM 解析 |
| **角色 B** | 场景痛点、交互设计与兜底、隐私保护、演示操作 |

### 12.2 演示流程（5 分钟）

| 时间 | 环节 | 操作 |
|------|------|------|
| 0:00-0:30 | 痛点引入（B） | 展示选课系统截图：只有课程名，没有详细信息和关系 |
| 0:30-1:30 | 课程解析演示（B 操作，A 解说） | 导入一份课程大纲 PDF → 系统自动解析 → 展示课程信息卡 |
| 1:30-2:30 | 课程地图（A） | 切换到图谱视图，展示先修关系网，展示学习路径推荐 |
| 2:30-3:30 | 情报官 + 学伴（B） | 展示推理链推送 + 学习建议对话 |
| 3:30-4:30 | 兜底 + 隐私（A） | 故意传一张模糊 PDF → 展示手动编辑回退 + 隐私模式切换 |
| 4:30-5:00 | 总结（A+B） | 一人一句收尾 |

### 12.3 常见 Q&A

| 问题 | 回答要点 |
|------|---------|
| 课程数据从哪来？ | 本校培养方案 PDF + 课程大纲，公开可获取 |
| 先修关系怎么保证准确？ | LLM 提取 + 人工编辑兜底，用户可随时修正 |
| 和选课系统有什么区别？ | 选课系统只告诉你有什么课，我们告诉你课和课之间有什么关系、怎么学最合理 |
| 为什么用三个 Agent？ | 解耦职责，各自独立升级；LangGraph 子图可复用；人工可介入 |
| 只针对你们学校？ | 本次定位就是服务本校，架构可扩展，但数据需重新适配 |

---

## 附录 A：快速环境配置

```bash
git clone https://github.com/your-org/graphcampus.git
cd graphcampus
python -m venv venv
.\venv\Scripts\activate  # Windows
pip install streamlit langgraph langchain openai networkx pandas beautifulsoup4 pdfplumber pyvis pillow gtts
cp .env.example .env
# 填入 OPENAI_API_KEY
streamlit run main.py
```

## 附录 B：关键依赖清单

| 包名 | 用途 |
|------|------|
| streamlit | 前端框架 |
| langgraph | 工作流引擎 |
| langchain + openai | LLM 调用 |
| networkx + pyvis | 知识图谱 + 可视化 |
| pandas | 数据分析 |
| beautifulsoup4 | 爬虫 |
| pdfplumber | PDF 解析 |
| pillow | 图片处理 |
| gtts | 语音合成 |

## 附录 C：本校数据源清单（待填写）

| 数据源 | URL | 更新频率 | 状态 |
|--------|-----|---------|------|
| 教务处培养方案 PDF | 待填写 | 每年 | ⏳ |
| 学院课程大纲 | 待填写 | 每学期 | ⏳ |
| 学院教师主页 | 待填写 | 不定期 | ⏳ |
| 学校讲座公告 | 待填写 | 每周 | ⏳ |
| 学科竞赛通知 | 待填写 | 按赛事 | ⏳ |

---

> 本文档由团队共同维护，随项目推进持续更新。
> 最后更新：2026 年 6 月
